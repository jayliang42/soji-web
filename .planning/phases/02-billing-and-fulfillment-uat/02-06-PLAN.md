---
phase: 02-billing-and-fulfillment-uat
plan: "06"
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - .planning/phases/02-billing-and-fulfillment-uat/02-UAT-EVIDENCE.md
autonomous: false
requirements: [BILL-01, BILL-02, BILL-03, BILL-04, BILL-05]
user_setup:
  - service: phase2-production-provider-gate
    why: "Production schema, exact-source deployment, Stripe test configuration, and authenticated later UAT require owner-authorized access."
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Existing production Supabase project; authorize secure Vercel installation/probe without pasting the value."
      - name: STRIPE_SECRET_KEY
        source: "Stripe test mode; authorize secure Vercel installation without pasting the value."
      - name: STRIPE_WEBHOOK_SECRET
        source: "Canonical Stripe test endpoint; authorize secure Vercel installation without pasting the value."
    dashboard_config:
      - task: "Authorize linked production Supabase migration/readiness access."
        location: "Existing Soji production project authenticated CLI/session"
      - task: "Authorize server-only secret changes and production deployment only in dedicated `soji-web`."
        location: "Vercel `soji-web` Production environment; unrelated projects remain untouched"
      - task: "Authorize Stripe test-mode API, webhook endpoint, and Customer Portal configuration access; no live payment."
        location: "Stripe test mode"
      - task: "Make labeled authenticated membership/product/Admin UAT browser sessions available to Plans 02-07 through 02-09."
        location: "Controlled canonical browser sessions; no addresses/passwords/codes/cookies/tokens in docs/chat"
must_haves:
  truths:
    - "The pre-push machine parser sees exactly one local-only reviewed migration and dry-run sees only that migration; after push versions match and dry-run is empty."
    - "Remote readiness proves the adjustment table/helper/RPCs/policy/grants/receipt allowlist from named booleans only."
    - "The exact clean committed HEAD containing Plans 02-01 through 02-05 passes release-input, detached production-build, and deploy-artifact gates before any Vercel/Stripe mutation, then deploys only to dedicated `soji-web`; evidence records public commit SHA, deployment ID/URL, canonical alias, and parity before UAT."
    - "D-21 D-22 D-24 D-25: secrets are handled only through authorized secure channels and no config/deploy check promotes provider rows PASS."
  artifacts:
    - path: ".planning/phases/02-billing-and-fulfillment-uat/02-UAT-EVIDENCE.md"
      provides: "Production schema/deployment identity and parity evidence"
      contains: "BILL-DB-SCHEMA-PARITY"
  key_links:
    - from: "20260726000000_subscription_billing_adjustments.sql"
      to: "production Supabase"
      via: "machine-checked dry-run and forward-only push"
      pattern: "subscription_billing_adjustments"
    - from: "verified release commit"
      to: "https://soji-web.vercel.app"
      via: "detached exact-HEAD worktree build/deploy and Vercel JSON inspection"
      pattern: "soji-web.vercel.app"
---

<objective>
Apply the reviewed production schema, configure secure server/provider access, and deploy/assert the exact verified commit before any provider-backed UAT.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-billing-and-fulfillment-uat/02-CONTEXT.md
@.planning/phases/02-billing-and-fulfillment-uat/02-VALIDATION.md
@.planning/phases/02-billing-and-fulfillment-uat/02-01-SUMMARY.md
@.planning/phases/02-billing-and-fulfillment-uat/02-02-SUMMARY.md
@.planning/phases/02-billing-and-fulfillment-uat/02-03-SUMMARY.md
@.planning/phases/02-billing-and-fulfillment-uat/02-04-SUMMARY.md
@.planning/phases/02-billing-and-fulfillment-uat/02-05-SUMMARY.md
@docs/phase-2-billing-and-fulfillment-uat.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS 4.0.3 L1 — V4, V5, V8, V10, V14"
high_severity_gate: "STOP before push, secret change, deploy, or evidence PASS whenever a HIGH gate fails."
threats:
  - threat_id: T-02-09
    category: Tampering
    severity: HIGH
    component: "Production migration"
    disposition: mitigate
    mitigation_plan: "Machine-parse versions/dry-run before and after; only reviewed forward migration; no seed/reset/repair/destructive flags."
  - threat_id: T-02-11
    category: Information Disclosure
    severity: HIGH
    component: "Production/provider secrets"
    disposition: mitigate
    mitigation_plan: "Explicit authorization, secure CLI/dashboard channels, no value output, tracked-file scan."
  - threat_id: T-02-14
    category: Tampering
    severity: HIGH
    component: "Source-to-deployment identity"
    disposition: mitigate
    mitigation_plan: "Prepare a clean detached exact-HEAD tree, pass release-input plus production-build/deploy-artifact gates before any Vercel/Stripe mutation, then link/deploy and verify Vercel JSON commit/deployment/alias."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Machine-check and push only the reviewed production migration</name>
  <files>.planning/phases/02-billing-and-fulfillment-uat/02-UAT-EVIDENCE.md</files>
  <read_first>supabase/migrations/20260726000000_subscription_billing_adjustments.sql, supabase/schema.sql, supabase/README.md, scripts/check-phase2-uat-evidence.mjs, docs/phase-2-billing-and-fulfillment-uat.md, .planning/phases/02-billing-and-fulfillment-uat/02-UAT-EVIDENCE.md</read_first>
  <action>With authenticated linked Supabase access, capture `supabase migration list` and `supabase db push --dry-run` into permission-restricted `/tmp/soji-phase2-migrations-before.txt` and `/tmp/soji-phase2-dryrun-before.txt`. Run validator `--prepush` and require local/remote arrays differ only by local `20260726000000`, and dry-run names only `20260726000000_subscription_billing_adjustments.sql`. Inspect that SQL; stop on any extra/destructive/seed scope. Run `supabase db push` once with no include-seed/reset/repair flag. Capture list and dry-run into `/tmp/soji-phase2-migrations-after.txt` and `/tmp/soji-phase2-dryrun-after.txt`; run `--postpush` and require exact local/remote parity plus zero pending migrations. Call validator `--production-schema`; require all named readiness booleans true. Promote only `BILL-DB-SCHEMA-PARITY` to PASS and record its schema-specific fields: local and remote migration version `20260726000000`, pending migration count `0`, dry-run pending count `0`, named table/constraints/helper/adjustment-RPC/reconciliation-RPC/RLS/grants/authenticated-no-write/receipt-allowlist booleans all true, ISO-8601 UTC observation, and canonical environment `https://soji-web.vercel.app`; include no secret or catalog detail. Keep all four permission-restricted pre/post capture files unchanged until this task's automated verification completes; do not delete or rewrite verification inputs in this task.</action>
  <verify>
    <automated>node scripts/check-phase2-uat-evidence.mjs --prepush --migration-list /tmp/soji-phase2-migrations-before.txt --dry-run /tmp/soji-phase2-dryrun-before.txt --expected-pending 20260726000000 &amp;&amp; node scripts/check-phase2-uat-evidence.mjs --postpush --migration-list /tmp/soji-phase2-migrations-after.txt --dry-run /tmp/soji-phase2-dryrun-after.txt &amp;&amp; node scripts/check-phase2-uat-evidence.mjs --production-schema &amp;&amp; corepack pnpm phase2:uat:check &amp;&amp; node scripts/check-phase2-uat-evidence.mjs --require-pass BILL-DB-SCHEMA-PARITY</automated>
  </verify>
  <acceptance_criteria>
    - Before push: exactly one expected pending migration/dry-run item.
    - After push: exact version parity and empty dry-run.
    - Remote table/helper/RPC/policy/grants/allowlist booleans all true.
    - `BILL-DB-SCHEMA-PARITY` is the only promoted row and its version/count/boolean/UTC/canonical fields pass the schema-specific `--require-pass` contract.
    - No seed/destructive action or secret output occurred.
  </acceptance_criteria>
  <done>Production schema matches the reviewed Phase 2 authority.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare and validate the exact clean release worktree</name>
  <files>None (permission-restricted temporary release metadata/worktree only)</files>
  <read_first>apps/web/.env.example, apps/web/vercel.json, .vercelignore, apps/web/src/app/api/health/ready/route.ts, scripts/check-deploy-artifact.mjs, scripts/check-phase2-uat-evidence.mjs, docs/phase-2-billing-and-fulfillment-uat.md</read_first>
  <action>After Task 1 verification has passed and its secret-free schema attestation is persisted in evidence, remove only the four named migration/dry-run capture files; this is the separate post-verification cleanup step. Resolve `releaseCommit=git rev-parse HEAD`, store only that public SHA in permission-restricted `/tmp/soji-phase2-release-commit.txt`, require Plans 02-01 through 02-05 commits are ancestors, and require every deployment input under apps/web/packages/root manifests/config is tracked and identical to that commit; otherwise STOP. Create a detached `mktemp -d` git worktree at exactly `releaseCommit` and store its path in permission-restricted `/tmp/soji-phase2-release-worktree.txt`. Run the fast `--release-inputs` gate. This task must not install/update Vercel or Stripe configuration, run `vercel link`, deploy, or capture Vercel inspection JSON; all production mutations remain blocked until Task 3 first passes the detached-worktree production build and `deploy:check` gates.</action>
  <verify>
    <automated>node scripts/check-phase2-uat-evidence.mjs --release-inputs --worktree-file /tmp/soji-phase2-release-worktree.txt --commit-file /tmp/soji-phase2-release-commit.txt</automated>
  </verify>
  <acceptance_criteria>
    - Deployment inputs are clean and exactly equal to recorded releaseCommit.
    - Fast release-input/static preflight proves exact detached commit, tracked required inputs/config, and no secret-like value in under 30 seconds.
    - No Vercel/Stripe configuration, link, deployment, or inspection command occurs in this task.
    - The detached worktree and public commit metadata remain available unchanged for Task 3.
  </acceptance_criteria>
  <done>The exact clean release input is prepared without production mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Assert canonical deployment identity, version, and remote parity before UAT</name>
  <files>.planning/phases/02-billing-and-fulfillment-uat/02-UAT-EVIDENCE.md</files>
  <read_first>scripts/check-phase2-uat-evidence.mjs, .planning/phases/02-billing-and-fulfillment-uat/02-UAT-EVIDENCE.md, apps/web/src/app/api/health/ready/route.ts, apps/web/src/app/api/me/route.ts</read_first>
  <action>As Task 3's first executable step, before any Vercel/Stripe configuration, `vercel link`, or deployment mutation, run the blocking sequence `node scripts/check-phase2-uat-evidence.mjs --release-inputs --worktree-file /tmp/soji-phase2-release-worktree.txt --commit-file /tmp/soji-phase2-release-commit.txt`, resolve `release_tree` from `/tmp/soji-phase2-release-worktree.txt`, then run `corepack pnpm --dir "$release_tree" --filter @soji/web build` followed by `corepack pnpm --dir "$release_tree" deploy:check`; STOP on any failure. Only after all three T-02-14 HIGH gates pass, use explicitly authorized secure Vercel/Stripe channels to install/update the production service-role key, Stripe test secret, and canonical endpoint signing secret, configure the Stripe test webhook URL/event set and Portal return access, link the detached worktree only to dedicated `soji-web`, deploy production from that worktree, and capture `vercel inspect https://soji-web.vercel.app --format=json` at permission-restricted `/tmp/soji-phase2-vercel-inspect.json`. Never deploy the mutable working tree or touch unrelated Vercel projects. Then run deployment validator against captured Vercel JSON with exact releaseCommit, canonical alias, production target, `soji-web` project, READY state, deployment ID/URL, and no alternate project. Run `--production-schema` and machine-parsed `--canonical-readiness https://soji-web.vercel.app`; canonical readiness must fail on non-200 health, `/api/me` source other than `supabase`, a non-test Stripe key, or any false canonical/Supabase/service-role/Stripe catalog/webhook boolean. Record public commit SHA, deployment ID/URL, canonical alias, UTC date, and parity booleans without secrets/full provider IDs. Do not promote catalog/provider rows.</action>
  <verify>
    <automated>node scripts/check-phase2-uat-evidence.mjs --deployment /tmp/soji-phase2-vercel-inspect.json --expected-commit-file /tmp/soji-phase2-release-commit.txt --expected-alias https://soji-web.vercel.app &amp;&amp; node scripts/check-phase2-uat-evidence.mjs --production-schema &amp;&amp; node scripts/check-phase2-uat-evidence.mjs --canonical-readiness https://soji-web.vercel.app &amp;&amp; corepack pnpm phase2:uat:check</automated>
  </verify>
  <acceptance_criteria>
    - Vercel JSON commit/deployment/project/alias matches exact verified release.
    - Release-input, detached production-build, and deploy-artifact gates all pass before the first Vercel/Stripe configuration, link, or deploy mutation.
    - Canonical public config and remote schema parity pass before Plan 02-07.
    - Evidence stores public identity/booleans only.
  </acceptance_criteria>
  <done>Provider UAT starts only on the exact canonical schema and deployment.</done>
</task>

</tasks>

<verification>
- [ ] Machine-parsed before/after migration and dry-run gates pass.
- [ ] Remote readiness booleans pass.
- [ ] Exact commit/deployment/project/alias identity passes.
- [ ] Task 3 first reruns `--release-inputs`, then runs `release_tree="$(tr -d '\n' < /tmp/soji-phase2-release-worktree.txt)" && corepack pnpm --dir "$release_tree" --filter @soji/web build && corepack pnpm --dir "$release_tree" deploy:check`; only after all pass may production configuration/link/deploy begin.
- [ ] No provider row is promoted and every HIGH threat is resolved.
</verification>

<success_criteria>
- Production schema and exact verified deployment are proven before test transactions.
- Secret/login authorization is consolidated here without a planned checkpoint.
</success_criteria>

<output>
Create `.planning/phases/02-billing-and-fulfillment-uat/02-06-SUMMARY.md`.
</output>
