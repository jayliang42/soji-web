---
phase: 02-billing-and-fulfillment-uat
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/app/api/checkout/subscription/route.ts
  - apps/web/src/lib/stripe-webhook.ts
  - apps/web/src/lib/stripe-reconciliation.ts
  - apps/web/tests/checkout-routes.test.ts
  - apps/web/tests/stripe-webhook-sync.test.ts
  - apps/web/tests/stripe-reconciliation.test.ts
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03, BILL-04, BILL-05]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04: Checkout accepts only plan plus opaque request intent; server owns Price/amount/URLs/user/metadata, reuses claims/idempotency/Customer, and fails before creating Customer or Session when lookup fails."
    - "D-07 through D-15: exact Invoice Payment classification selects product versus membership adjustment state machines without weakening delayed fulfillment or refund/dispute policy."
    - "D-17 D-18 D-19 D-21: receipt-first leases, stable ignored/failed outcomes, original-event Retry, and authoritative reconciliation remain secret-free."
  artifacts:
    - path: "apps/web/tests/checkout-routes.test.ts"
      provides: "Negative authority, Customer continuity, claim, and idempotency contracts"
    - path: "apps/web/src/lib/stripe-webhook.ts"
      provides: "InvoicePayment classification and adjustment dispatch"
    - path: "apps/web/src/lib/stripe-reconciliation.ts"
      provides: "Later paid InvoicePayment reconciliation"
  key_links:
    - from: "subscription checkout route"
      to: "claimSubscriptionCheckout"
      via: "opaque request intent and reusable Stripe idempotency key"
      pattern: "claimSubscriptionCheckout"
    - from: "stripe-webhook.ts"
      to: "stripe.invoicePayments.list"
      via: "exact PaymentIntent mapping with limit 2"
      pattern: "invoicePayments.list"
    - from: "stripe-webhook.ts"
      to: "sync_stripe_subscription_adjustment"
      via: "validated current Subscription metadata"
      pattern: "sync_stripe_subscription_adjustment"
---

<objective>
Lock server-owned Checkout negative contracts, classify Stripe Invoice Payments, dispatch product/membership reversals, and preserve receipt recovery.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-billing-and-fulfillment-uat/02-CONTEXT.md
@.planning/phases/02-billing-and-fulfillment-uat/02-RESEARCH.md
@.planning/phases/02-billing-and-fulfillment-uat/02-VALIDATION.md
@.planning/phases/02-billing-and-fulfillment-uat/02-PATTERNS.md
@.planning/phases/02-billing-and-fulfillment-uat/02-01-SUMMARY.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS 4.0.3 L1 — V4, V5, V8, V10, V14"
high_severity_gate: "STOP and do not commit while any HIGH threat lacks a focused passing test."
threats:
  - threat_id: T-02-01
    category: Spoofing
    severity: HIGH
    component: "Checkout/payment ownership"
    disposition: mitigate
    mitigation_plan: "Ignore/reject client authority; require authenticated user, server lookup, exact InvoicePayment/Invoice/Subscription evidence."
  - threat_id: T-02-04
    category: Tampering
    severity: HIGH
    component: "Adjustment dispatch/recovery"
    disposition: mitigate
    mitigation_plan: "Mutually exclusive product/subscription RPCs, observation ordering, original-event Retry, authoritative paid-payment reconciliation."
  - threat_id: T-02-07
    category: Information Disclosure
    severity: HIGH
    component: "Receipts/errors/logs"
    disposition: mitigate
    mitigation_plan: "Bounded references and stable errors only; tests reject raw payload/customer/secret output."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove Checkout rejects client authority and preserves continuity</name>
  <files>apps/web/src/app/api/checkout/subscription/route.ts, apps/web/tests/checkout-routes.test.ts</files>
  <read_first>apps/web/src/app/api/checkout/subscription/route.ts, apps/web/tests/checkout-routes.test.ts, apps/web/src/lib/subscription-checkout.ts, apps/web/src/lib/stripe-customer.ts, apps/web/src/lib/stripe-price-validation.ts, .planning/phases/02-billing-and-fulfillment-uat/02-CONTEXT.md</read_first>
  <action>Add negative route tests that submit forged `priceId`, `amount`, `currency`, `successUrl`, `cancelUrl`, `userId`, `customerId`, and metadata. Require rejection or complete ignoring in favor of authenticated user, `getPlanByTier` lookup key/amount, canonical return URLs, and server metadata. Assert Customer lookup failure calls neither `customers.create` nor `checkout.sessions.create`. Assert the newest bound Customer is reused; open/in-progress membership blocks a second intent; the same opaque request intent reuses its database claim and Stripe idempotency key/Session; a different ambiguous retry cannot create another Session. Modify the route only where these tests expose a contract gap.</action>
  <verify>
    <automated>corepack pnpm --filter @soji/web exec vitest run tests/checkout-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every forged field is explicitly asserted ignored/rejected.
    - Customer lookup failure creates neither Customer nor Session.
    - Claim/idempotency reuse and single-live-membership behavior are value-level assertions.
    - Focused test exits 0 in under 30 seconds.
  </acceptance_criteria>
  <done>Checkout authority and Customer continuity satisfy D-01 through D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify Invoice Payments and dispatch refunds/disputes</name>
  <files>apps/web/src/lib/stripe-webhook.ts, apps/web/tests/stripe-webhook-sync.test.ts</files>
  <read_first>apps/web/src/lib/stripe-webhook.ts, apps/web/tests/stripe-webhook-sync.test.ts, apps/web/node_modules/stripe/types/InvoicePaymentsResource.d.ts, apps/web/node_modules/stripe/types/InvoicePayments.d.ts, apps/web/node_modules/stripe/types/Invoices.d.ts, apps/web/src/lib/supabase/database.types.ts, .planning/phases/02-billing-and-fulfillment-uat/02-RESEARCH.md</read_first>
  <action>Preserve product UUID metadata fast path. Otherwise resolve PaymentIntent, call `invoicePayments.list({ payment: { type: "payment_intent", payment_intent: id }, limit: 2 })`, require one non-deleted Invoice with `subscription_details` parent, retrieve current Subscription, and validate Soji user/plan metadata. Zero non-Soji evidence is stable ignored; ambiguous/malformed evidence fails before RPC. Route cumulative partial/full subscription refunds and all five dispute event types to `sync_stripe_subscription_adjustment`; keep product RPCs product-only. Add only bounded direct receipt references. Test product/subscription/zero/ambiguous/deleted/null-parent/invalid-metadata/Charge-fallback cases and exact RPC arguments.</action>
  <verify>
    <automated>corepack pnpm --filter @soji/web exec vitest run tests/stripe-webhook-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exact InvoicePayment mapping is required; email/amount/Customer/local-recency inference is absent.
    - Product and membership dispatch are mutually exclusive.
    - D-07 through D-15 transitions and bounded receipt keys are value-tested.
    - Focused test exits 0 in under 30 seconds.
  </acceptance_criteria>
  <done>Refund/dispute events reach the correct atomic state machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile paid evidence and retain Retry/lease contracts</name>
  <files>apps/web/src/lib/stripe-reconciliation.ts, apps/web/tests/stripe-reconciliation.test.ts</files>
  <read_first>apps/web/src/lib/stripe-reconciliation.ts, apps/web/tests/stripe-reconciliation.test.ts, apps/web/src/app/api/admin/billing-events/[id]/retry/route.ts, apps/web/src/app/api/admin/billing-events/reconcile/route.ts, apps/web/tests/stripe-webhook-route.test.ts, apps/web/tests/billing-processing.test.ts, apps/web/tests/admin-billing-retry-route.test.ts, apps/web/tests/admin-billing-reconcile-route.test.ts</read_first>
  <action>For each exact reconciled subscription, narrow/retrieve latest Invoice, list paid Invoice Payments by invoice with limit 2, require one PaymentIntent-backed paid result and its paid timestamp, then call `reconcile_stripe_subscription_paid_payment`. Active status alone cannot unblock. Preserve sub_/cus_ validation, customer pagination, missing-subscription closure, original Stripe Event Retry, synthetic reconciliation identifier Retry, and token-owned leases. Add later/same/older/no/ambiguous/Charge-only paid-payment tests.</action>
  <verify>
    <automated>corepack pnpm --filter @soji/web exec vitest run tests/stripe-reconciliation.test.ts tests/billing-processing.test.ts tests/admin-billing-retry-route.test.ts tests/admin-billing-reconcile-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Only exact later paid PaymentIntent evidence can supersede full refund.
    - Retry and reconciliation remain distinct, idempotent, and lease-safe.
    - Focused recovery tests exit 0 in under 30 seconds.
  </acceptance_criteria>
  <done>Processor recovery is authoritative without promoting mocks to provider evidence.</done>
</task>

</tasks>

<verification>
- [ ] Three focused commands complete under 30 seconds each.
- [ ] Full Web/DB suites run only at the wave gate.
- [ ] No provider mock marks a live UAT row PASS.
- [ ] Every HIGH threat is resolved.
</verification>

<success_criteria>
- Checkout ignores/rejects every client authority field and reuses claims/Customer/idempotency.
- Refund/dispute classification is exact and recoverable.
- BILL-01 through BILL-05 application contracts are ready for presentation.
</success_criteria>

<output>
Create `.planning/phases/02-billing-and-fulfillment-uat/02-02-SUMMARY.md`.
</output>
