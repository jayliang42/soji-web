---
phase: 02-billing-and-fulfillment-uat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260726000000_subscription_billing_adjustments.sql
  - supabase/schema.sql
  - supabase/tests/subscription_billing_adjustments.sql
  - apps/web/src/lib/supabase/database.types.ts
autonomous: true
requirements: [BILL-03, BILL-05]
user_setup: []
must_haves:
  truths:
    - "D-07 D-08 D-09: only active/trialing subscriptions without an unresolved block provision access; open disputes pause, eligible wins restore, and lost remains blocking."
    - "D-10 D-11 D-12: partial refunds preserve otherwise-eligible access, full refunds remain blocking until later verified payment/reconciliation, subscription refresh cannot erase a block, and Soji never mutates provider collection/cancellation."
    - "D-17 D-18 D-21: receipt/lease authority remains intact and normalized rows plus bounded receipt references store no customer data or provider payload."
  artifacts:
    - path: "supabase/migrations/20260726000000_subscription_billing_adjustments.sql"
      provides: "Forward adjustment schema, shared entitlement helper, service-role sync/reconciliation/readiness RPCs"
      contains: "get_phase2_billing_schema_readiness"
    - path: "supabase/tests/subscription_billing_adjustments.sql"
      provides: "Focused pgTAP state-machine/security/readiness coverage"
    - path: "apps/web/src/lib/supabase/database.types.ts"
      provides: "Generated adjustment and RPC contracts"
  key_links:
    - from: "sync_stripe_subscription_state"
      to: "recompute_stripe_subscription_access"
      via: "shared stored-state eligibility under subscription/user locks"
      pattern: "recompute_stripe_subscription_access"
    - from: "sync_stripe_subscription_adjustment"
      to: "subscription_billing_adjustments"
      via: "ordered idempotent service-role upsert"
      pattern: "provider_adjustment_id"
---

<objective>
Create the atomic membership adjustment authority, focused pgTAP contract, generated types, and remote schema-readiness proof needed by later Stripe and production waves.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-billing-and-fulfillment-uat/02-CONTEXT.md
@.planning/phases/02-billing-and-fulfillment-uat/02-RESEARCH.md
@.planning/phases/02-billing-and-fulfillment-uat/02-VALIDATION.md
@.planning/phases/02-billing-and-fulfillment-uat/02-PATTERNS.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS 4.0.3 L1 — V4, V5, V8, V10, V14"
high_severity_gate: "STOP and do not commit while any HIGH threat lacks a passing focused mitigation."
threats:
  - threat_id: T-02-02
    category: Tampering
    severity: HIGH
    component: "Adjustment ordering/access recomputation"
    disposition: mitigate
    mitigation_plan: "Advisory locks, provider uniqueness, observed-time/stable-ID ordering, monotonic lost/full-refund rules, and focused pgTAP."
  - threat_id: T-02-03
    category: Elevation of Privilege
    severity: HIGH
    component: "Adjustment/reconciliation/readiness RPCs"
    disposition: mitigate
    mitigation_plan: "Service-role-only execution, owner/Admin read RLS, authenticated no-write assertions."
  - threat_id: T-02-09
    category: Tampering
    severity: HIGH
    component: "Forward migration and declarative parity"
    disposition: mitigate
    mitigation_plan: "One immutable forward migration, schema/type parity, focused tests; production push is reserved for Plan 02-06."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write a focused RED pgTAP contract</name>
  <files>supabase/tests/subscription_billing_adjustments.sql</files>
  <read_first>supabase/migrations/20260715050000_order_stripe_state_events.sql, supabase/migrations/20260715070000_sync_product_disputes.sql, supabase/migrations/20260715040000_sync_product_refunds.sql, supabase/tests/database_access.sql, .planning/phases/02-billing-and-fulfillment-uat/02-CONTEXT.md, .planning/phases/02-billing-and-fulfillment-uat/02-PATTERNS.md</read_first>
  <action>Create a self-contained pgTAP file for table constraints, multiple adjustments, duplicate/older/equal-time replay, open/won/lost disputes, partial/full refunds, later-active-refresh resistance, verified-later-payment supersession, atomic entitlements/tier, rollback, RLS, grants, bounded receipt keys, and readiness RPC booleans. Reuse existing JWT/fixture helpers. Give the two RED existence assertions unique TAP descriptions exactly `phase2 RED missing adjustment table` and `phase2 RED missing adjustment RPC`; they must emit named `not ok` lines before implementation. The RED check must prove the TAP plan was reached and those two assertions failed, while rejecting syntax/connection/setup failures and any `Bail out!`; never invert or run the full database suite.</action>
  <verify>
    <automated>bash -o pipefail -c 'set +e; output=$(corepack pnpm --config.registry=https://registry.npmjs.org dlx supabase@2.109.1 test db --local supabase/tests/subscription_billing_adjustments.sql 2&gt;&amp;1); status=$?; printf "%s\n" "$output" | rg -q "^1\\.\\.[0-9]+$"; printf "%s\n" "$output" | rg -q "^not ok [0-9]+ - phase2 RED missing adjustment table$"; printf "%s\n" "$output" | rg -q "^not ok [0-9]+ - phase2 RED missing adjustment RPC$"; if printf "%s\n" "$output" | rg -qi "Bail out!|syntax error|could not connect|connection refused|failed to connect|password authentication failed|database .* does not exist"; then exit 1; fi; test "$status" -ne 0'</automated>
  </verify>
  <acceptance_criteria>
    - TAP emits its numeric plan and both exact named `not ok` assertions.
    - Syntax, connection, authentication, missing-database, setup, and `Bail out!` failures are explicitly rejected.
    - Raw artifact substrings or unrelated/full-suite failures cannot satisfy the RED gate.
  </acceptance_criteria>
  <done>The focused RED contract encodes D-07 through D-12 and security boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement adjustment, shared access, receipt allowlist, and readiness RPC</name>
  <files>supabase/migrations/20260726000000_subscription_billing_adjustments.sql, supabase/schema.sql, supabase/tests/subscription_billing_adjustments.sql</files>
  <read_first>supabase/schema.sql, supabase/tests/subscription_billing_adjustments.sql, supabase/migrations/20260715050000_order_stripe_state_events.sql, supabase/migrations/20260714100000_minimize_stripe_billing_event_payload.sql, .planning/phases/02-billing-and-fulfillment-uat/02-RESEARCH.md</read_first>
  <action>Create one immutable forward migration and exact declarative mirror. Add normalized adjustment identity/state/amount/currency/observation/supersession fields; internal `recompute_stripe_subscription_access`; service-role-only `sync_stripe_subscription_adjustment` and `reconcile_stripe_subscription_paid_payment`; owner/Admin SELECT RLS; authenticated no-write; bounded `chargeId/customerId/disputeId/paymentId/subscriptionId` receipt keys. Add service-role-only `get_phase2_billing_schema_readiness()` returning named booleans for table, constraints, helper, both RPCs, RLS policy, grants, authenticated no-write, and receipt allowlist so Plan 02-06 can assert remote objects without exposing catalog rows. Keep lost/full refund monotonic and never mutate Stripe billing.</action>
  <verify>
    <automated>corepack pnpm --config.registry=https://registry.npmjs.org dlx supabase@2.109.1 test db --local supabase/tests/subscription_billing_adjustments.sql &amp;&amp; corepack pnpm db:schema:check</automated>
  </verify>
  <acceptance_criteria>
    - Focused pgTAP and schema parity exit 0.
    - Both subscription and adjustment sync call the same recomputation helper.
    - Readiness returns all required named booleans locally and is not executable by anon/authenticated.
    - No migration older than the new forward migration changes.
  </acceptance_criteria>
  <done>The database is the atomic membership adjustment/access authority.</done>
</task>

<task type="auto">
  <name>Task 3: Generate exact Supabase TypeScript contracts</name>
  <files>apps/web/src/lib/supabase/database.types.ts</files>
  <read_first>scripts/sync-supabase-types.mjs, package.json, apps/web/src/lib/supabase/database.types.ts, supabase/migrations/20260726000000_subscription_billing_adjustments.sql</read_first>
  <action>Run the repository generator after local migration application; never hand-edit. Require adjustment relationships and exact argument/return types for adjustment, paid-payment reconciliation, and schema-readiness RPCs.</action>
  <verify>
    <automated>corepack pnpm db:types &amp;&amp; corepack pnpm db:types:check &amp;&amp; corepack pnpm --filter @soji/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Generated types include the table and three RPC contracts.
    - Type parity and Web typecheck exit 0.
    - Production remains untouched.
  </acceptance_criteria>
  <done>Database and application contracts are synchronized for Plan 02-02.</done>
</task>

</tasks>

<verification>
- [ ] Focused pgTAP, schema, generated-type, and Web type checks pass.
- [ ] Full database suite runs only as the wave gate after task-local checks.
- [ ] Every HIGH threat is resolved; production is unchanged.
</verification>

<success_criteria>
- Adjustment history/order/access policy is atomic and service-role controlled.
- Local and remote-readiness contracts are testable without secret output.
- BILL-03/BILL-05 database authority is ready for processor integration.
</success_criteria>

<output>
Create `.planning/phases/02-billing-and-fulfillment-uat/02-01-SUMMARY.md`.
</output>
