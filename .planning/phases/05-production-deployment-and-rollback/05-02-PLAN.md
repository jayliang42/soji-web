---
phase: 05-production-deployment-and-rollback
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-phase5-container.mjs
  - scripts/check-phase5-container.test.mjs
  - package.json
autonomous: true
requirements: [DEPLOY-01, DEPLOY-03]
must_haves:
  truths:
    - "D-04: the Docker standalone remains a non-root, secret-free, loopback-only portable fallback."
    - "D-11 through D-13: update/rollback mechanics restore an immutable prior artifact while database history remains outside the drill."
    - "The drill owns and may clean up only exact temporary image/container names it created."
  artifacts:
    - path: "scripts/check-phase5-container.mjs"
      provides: "Bounded image inspection and local prior/candidate/rollback drill"
    - path: "package.json"
      provides: "Repeatable local and CI-compatible container verification commands"
  key_links:
    - from: "scripts/check-phase5-container.mjs"
      to: "Dockerfile"
      via: "non-root user, liveness, standalone entry, and runtime secret boundary"
      pattern: "nextjs"
---

<objective>
Turn the existing one-start container smoke into a safe immutable update/rollback drill that
proves the portable fallback without claiming Vercel or database rollback truth.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-deployment-and-rollback/05-CONTEXT.md
@.planning/phases/05-production-deployment-and-rollback/05-RESEARCH.md
@.planning/phases/05-production-deployment-and-rollback/05-VALIDATION.md
@.planning/phases/05-production-deployment-and-rollback/05-PATTERNS.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS L1 — V8 Data Protection, V14 Configuration"
high_severity_gate: "STOP if an arbitrary Docker resource can be stopped/removed, secret-like config/history is accepted, a non-root check is missing, or the drill suggests database rollback."
threats:
  - threat_id: T-05-03
    category: Information Disclosure / Privilege
    severity: HIGH
    component: "Standalone image"
    disposition: mitigate
    mitigation_plan: "Inspect final user/config/history and standalone contents; reject environment files and server-secret assignments."
  - threat_id: T-05-04
    category: Unsafe Resource Operation
    severity: HIGH
    component: "Container rollback drill"
    disposition: mitigate
    mitigation_plan: "Generate validated exact names, track ownership, use loopback ports, and permit cleanup only for recorded resources."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define secret-safe non-root image and bounded drill planning</name>
  <files>scripts/check-phase5-container.mjs, scripts/check-phase5-container.test.mjs</files>
  <read_first>Dockerfile, scripts/check-deploy-artifact.mjs, .github/workflows/ci.yml, docs/deployment.md, package.json, .planning/phases/05-production-deployment-and-rollback/05-CONTEXT.md</read_first>
  <action>Add RED Node tests, then implement an import-safe module with pure `validateImageInspection`, `createRollbackDrillPlan`, and `validateOwnedResourceName`. Require final image user exactly `nextjs`, entry `node apps/web/server.js`, port `3000`, liveness healthcheck, no environment filenames, and no assignment for `SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `CRON_SECRET`, or `OPS_ALERT_WEBHOOK_URL` in final Config.Env, labels, or non-secret build history. Generate names matching `soji-phase5-(prior|candidate)-[a-f0-9]{8}` and loopback ports within a fixed test range. The ordered plan is prior start/health, candidate start/health, candidate selection, prior stop, candidate health, candidate stop, prior restart/health, then exact owned cleanup. Reject empty, user-provided arbitrary, duplicated, broad, shell-metacharacter, non-prefix, or unrecorded targets. The pure plan contains Docker argv arrays, never shell strings, and includes no database or provider command.</action>
  <verify><automated>node --test scripts/check-phase5-container.test.mjs</automated></verify>
  <acceptance_criteria>
    - Tests reject root/numeric/empty user, missing healthcheck/entry, secret assignments, environment files, unsafe names, non-loopback publishing, duplicate ports, and unowned cleanup.
    - Valid command plan contains only `docker image inspect`, `docker run`, `docker inspect`, `docker stop`, `docker start`, and exact `docker rm` argv arrays.
    - No plan command contains Vercel, Supabase, migration, database restore, environment value, shell operator, glob, or unresolved variable.
    - Module import is silent and starts no process.
  </acceptance_criteria>
  <done>Container inspection and rollback mechanics are pure, bounded, and adversarially tested.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the local immutable update/rollback drill with CI-compatible commands</name>
  <files>scripts/check-phase5-container.mjs, scripts/check-phase5-container.test.mjs, package.json</files>
  <read_first>scripts/check-phase5-container.mjs, scripts/check-phase5-container.test.mjs, Dockerfile, .github/workflows/ci.yml, package.json, scripts/check-deploy-artifact.mjs</read_first>
  <action>Add CLI modes `--inspect IMAGE` and explicit `--drill --prior-image IMAGE --candidate-image IMAGE`. Execute commands with `execFileSync`/`spawnSync` argv and fixed timeouts; poll `/api/health` on the two loopback ports, never print response bodies, and register cleanup for only created containers. The integration command requires both images to exist and refuses `latest`. Add `phase5:container:test` for Node tests and `phase5:container:drill` for the real Docker integration. The commands must accept immutable SHA-derived prior/candidate tags and be directly callable from the existing CI container job without modifying the user-owned untracked workflow; no command runs Docker during package install or import.</action>
  <verify><automated>node --test scripts/check-phase5-container.test.mjs &amp;&amp; corepack pnpm phase5:container:drill</automated></verify>
  <acceptance_criteria>
    - Unit tests inject a fake runner and prove timeout/failure cleanup order without using Docker.
    - Real drill exits 0 only after both initial health checks, candidate selection health, and restored-prior health.
    - Docker inspection proves `nextjs` final user and no forbidden runtime/build-history material.
    - The CI-compatible command accepts immutable SHA-derived tags, uses loopback publishing, bounded timeout, and exact-name cleanup.
    - Package scripts distinguish fast unit test from explicit Docker integration.
  </acceptance_criteria>
  <done>The portable fallback can update and restore an immutable prior process under automated CI proof.</done>
</task>

</tasks>

<verification>
- [ ] Container unit and real Docker drill pass.
- [ ] Non-root/env/artifact checks remain green.
- [ ] CI-compatible commands use exact cleanup and never touch database/provider state.
</verification>

<success_criteria>
- DEPLOY-01 and the mechanical portion of DEPLOY-03 are repository-verifiable and repeatable.
</success_criteria>

<output>Create `05-02-SUMMARY.md`.</output>
