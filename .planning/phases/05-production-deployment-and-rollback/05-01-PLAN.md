---
phase: 05-production-deployment-and-rollback
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-phase5-release.mjs
  - scripts/check-phase5-release.test.mjs
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "D-02 D-03: only one clean detached exact commit can become a staged production candidate; inspection must prove no canonical alias before promotion."
    - "D-06 through D-10: bounded liveness/readiness/public smoke fails closed without retaining response bodies, credentials, identities, or target URLs."
    - "D-16 D-17 D-21: fixtures and hand-edited text cannot satisfy a live deployment state."
  artifacts:
    - path: "scripts/check-phase5-release.mjs"
      provides: "Import-safe release-input, staged/current inspection, transition, and body-free smoke validation"
    - path: "scripts/check-phase5-release.test.mjs"
      provides: "Adversarial exact-commit, state, URL, privacy, and CLI proof"
  key_links:
    - from: "scripts/check-phase5-release.mjs"
      to: "scripts/check-phase2-uat-evidence.mjs"
      via: "reuse of exact release-input and deployment parser authority"
      pattern: "check-phase2-uat-evidence"
---

<objective>
Create the deterministic, non-mutating release gate that proves an exact staged/current
deployment identity and a bounded production smoke result before any provider mutation.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-production-deployment-and-rollback/05-CONTEXT.md
@.planning/phases/05-production-deployment-and-rollback/05-RESEARCH.md
@.planning/phases/05-production-deployment-and-rollback/05-VALIDATION.md
@.planning/phases/05-production-deployment-and-rollback/05-PATTERNS.md
@.planning/phases/05-production-deployment-and-rollback/05-UI-SPEC.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS L1 — V4 Access Control, V8 Data Protection, V14 Configuration"
high_severity_gate: "STOP if dirty/wrong source can pass, staged proof permits the canonical alias, smoke follows redirects, or output can retain bodies/credentials/identity/full deployment IDs."
threats:
  - threat_id: T-05-01
    category: Supply Chain / Configuration
    severity: HIGH
    component: "Exact release candidate"
    disposition: mitigate
    mitigation_plan: "Reuse the Phase 2 clean-detached input authority and require exact commit equality in every inspection."
  - threat_id: T-05-02
    category: SSRF / Information Disclosure
    severity: HIGH
    component: "Release smoke probe"
    disposition: mitigate
    mitigation_plan: "Allow only the canonical origin or exact generated Vercel deployment host, refuse credentials/path/query/fragment/redirects, and project allowlisted status/header/readiness booleans only."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock exact staged/current deployment and transition contracts</name>
  <files>scripts/check-phase5-release.mjs, scripts/check-phase5-release.test.mjs</files>
  <read_first>scripts/check-phase2-uat-evidence.mjs, scripts/check-phase2-uat-evidence.test.mjs, docs/phase-2-billing-and-fulfillment-uat.md, apps/web/vercel.json, .vercelignore, apps/web/next.config.ts, .planning/phases/05-production-deployment-and-rollback/05-CONTEXT.md</read_first>
  <action>Add RED Node tests, then implement an import-safe Phase 5 module that imports `validateReleaseInputs` and `parseDeploymentInspection` from the Phase 2 authority. Define fixed lifecycle states `staged`, `current`, `rolled_back`, and `repromoted`. A staged inspection must be project `soji-web`, target `production`, state `READY`, exact full expected commit, valid public `dpl_` identifier, generated root HTTPS Vercel deployment host, and must NOT contain `https://soji-web.vercel.app` in aliases. A current inspection requires that exact canonical alias. Normalize retained evidence to commit, lifecycle state, and the final eight alphanumeric characters of the deployment ID; never persist the generated URL or full ID. Validate transitions only in order: prior current + candidate staged → candidate current → prior rolled_back → candidate repromoted. Reject identical prior/candidate IDs, wrong commit, unknown/duplicate/missing state, hand-edited non-JSON inspection, wrong project/target/state/alias, URL credentials/port/path/query/fragment, and transition skipping. Add CLI modes using separate permission-restricted commit/worktree/inspection files; default/import performs no network or provider mutation and prints stable aggregate confirmation only.</action>
  <verify><automated>node --test scripts/check-phase5-release.test.mjs</automated></verify>
  <acceptance_criteria>
    - Tests prove dirty or non-detached input, expected-commit mismatch, wrong project/target/state, malformed URL/ID, and missing provider commit exit nonzero.
    - A staged fixture with the canonical alias fails; a current fixture without it fails.
    - Normalized output has exactly `commit`, `deploymentSuffix`, and `lifecycleState`; JSON stringification contains no generated URL or full `dpl_` identifier.
    - Only the four-state ordered rollback/re-promotion sequence passes.
    - Module import emits no stdout/stderr and invokes no filesystem, network, Vercel, or Git mutation.
  </acceptance_criteria>
  <done>One exact candidate and its promotion/rollback identities are machine-verifiable without provider mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded staged/canonical liveness, readiness, and public smoke</name>
  <files>scripts/check-phase5-release.mjs, scripts/check-phase5-release.test.mjs</files>
  <read_first>scripts/check-phase5-release.mjs, scripts/check-phase5-release.test.mjs, apps/web/src/app/api/health/route.ts, apps/web/src/app/api/health/ready/route.ts, apps/web/tests/health-routes.test.ts, apps/web/e2e/public-pages.spec.ts, apps/web/next.config.ts, .planning/phases/05-production-deployment-and-rollback/05-UI-SPEC.md</read_first>
  <action>Add RED tests and implement `validateReleaseOrigin` plus an injectable `probeReleaseTarget`. Accept only exact canonical origin or a single-label generated `*.vercel.app` HTTPS origin with no username/password/port/path/query/fragment; the generated origin is supplied only from a mode-0600 file. Fetch `/api/health`, `/api/health/ready`, `/`, `/pricing`, `/products`, `/library`, `/office-hours`, `/support`, `/privacy`, `/terms`, `/refund-policy`, and `/financial-disclaimer` using GET, `cache: "no-store"`, `redirect: "error"`, and a ten-second abort. Require liveness 200; readiness 200 with source-free named booleans all true and `ready: true`; pages 200; HTML response type; and CSP, HSTS, Referrer-Policy, X-Content-Type-Options, and X-Frame-Options on `/`. Reject any `Demo preview`/`Preview data` marker while scanning in-memory page text. Return only route/status and named boolean outcomes; do not return/store body, headers, origin, redirect, exception message, cookie, identity, or provider data. CLI output is one stable counts/result line.</action>
  <verify><automated>node --test scripts/check-phase5-release.test.mjs &amp;&amp; corepack pnpm --filter @soji/web test -- tests/health-routes.test.ts</automated></verify>
  <acceptance_criteria>
    - Origin tests reject HTTP, nested Vercel host, unrelated host, credentials, port, path, query, and fragment.
    - Probe tests assert no-store, redirect error, ten-second abort, fixed routes, and zero request credentials.
    - Any non-200, missing readiness boolean, readiness detail field, missing security header, demo marker, redirect, timeout, or fetch failure yields stable `ok: false`.
    - Serialized return/output contains no HTML, response header value, target origin, generated deployment hostname, exception message, or private sentinel.
    - Focused Node and Web health tests pass.
  </acceptance_criteria>
  <done>Staged and canonical public release truth can be probed safely without turning response detail into evidence.</done>
</task>

</tasks>

<verification>
- [ ] Phase 2 exact release authority is reused, not weakened or copied.
- [ ] Staged/current/rollback transition and origin/smoke tests pass.
- [ ] Default CLI modes are read-only and output only allowlisted aggregate truth.
</verification>

<success_criteria>
- DEPLOY-01/DEPLOY-02 have deterministic exact-candidate and staged smoke gates before any live mutation plan.
</success_criteria>

<output>Create `05-01-SUMMARY.md`.</output>
