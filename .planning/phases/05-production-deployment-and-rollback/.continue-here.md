---
context: phase
phase: 05-production-deployment-and-rollback
task: 1
total_tasks: 2
status: paused
last_updated: 2026-08-09T00:06:44Z
---

# Soji 收尾交接

## 当前状态

Phase 5 的 Plan 01–03 已完成。Plan 04 是唯一剩余计划，包含两个必须在同一个 owner-authorized session 中完成的 checkpoint task：

1. 关闭 Phase 1–4 的 production identity、billing、content/policy 和 operations 真实观察。
2. 用 exact clean detached commit 完成 staged deploy、smoke、promotion、rollback、prior smoke 和 re-promotion。

本地应用、UI、测试、生产构建、standalone artifact、容器回滚演练、证据 privacy gate 和文档 gate 已完成。剩余不是普通代码 bug，而是生产凭据、第三方 provider、真实账号和部署权限带来的外部观察。

## 已完成

- Phase 5 Plan 01：精确 release candidate、staged/current/rollback/re-promotion 状态机和 body-free smoke gate。主要实现 commit：`fa9e41a`。
- Phase 5 Plan 02：non-root standalone image、secret boundary、immutable container update/rollback drill。主要实现 commit：`ee2aaff`，integration command：`53882d0`。
- Phase 5 Plan 03：58 行固定证据 ledger（10 条自动化 PASS + 48 条 owner/provider PENDING）、唯一 consolidated checkpoint 和 exact-commit release regression。主要实现 commit：`c3d990e`、`9a02675`。
- Phase 3 和 Phase 4 已完成；最近的 UI quick tasks 已记录在 `.planning/STATE.md`。

## 剩余工作

详细清单见 `.planning/REMAINING-GAPS.md`。摘要如下：

- Phase 1：6 条 identity/Admin rows。
- Phase 2：25 条 schema/catalog/Checkout/receipt/product/refund/dispute rows。
- Phase 3：6 条 content/office-hours/support/policy/Terms rows。
- Phase 4：3 条 operations receiver/scheduler/Admin Billing rows。
- Phase 5：8 条 environment/staged/promotion/smoke/rollback/re-promotion rows。

合计 48 条 owner/provider rows。不要在没有直接观察时改成 PASS。

## 必读文档

1. `.planning/REMAINING-GAPS.md`
2. `.planning/NEXT-CONVERSATION-PROMPT.md`
3. `.planning/ROADMAP.md`
4. `.planning/REQUIREMENTS.md`
5. `.planning/STATE.md`
6. `.planning/phases/05-production-deployment-and-rollback/05-04-PLAN.md`
7. `docs/phase-4-experience-and-operations-acceptance.md`
8. `docs/phase-5-production-deployment-and-rollback.md`
9. `.planning/phases/05-production-deployment-and-rollback/05-UAT-EVIDENCE.md`

## 强制约束

- 只用 Codex；不使用 Claude 或 Ultraplan。
- 只向用户提出一次 consolidated owner/login checkpoint，不要拆成多轮登录问题。
- 不要求用户把 secret、token、完整 ID、邮箱、session URL、raw response 或 webhook body 贴进聊天或终端。
- 保留现有 dirty worktree；不要 reset、checkout、clean 或整体 stage。
- 不再做泛化安全审计；只执行发布和功能闭环必需的 fail-closed gate。
- 保持 Vercel 为 canonical production，Docker 只是已验证的 fallback。
- 应用 rollback 不等于数据库 rollback；migration history 必须 forward-only。
- staged candidate 必须先无 canonical alias、通过 smoke，再 promotion；promotion、rollback 和 re-promotion 都必须是同一个 inspected artifact。

## 恢复后的第一步

先运行：

```sh
git status --short
corepack pnpm phase5:release:check
corepack pnpm phase1:uat:check
corepack pnpm phase2:uat:check
corepack pnpm phase3:uat:check
corepack pnpm phase4:uat:check
corepack pnpm phase5:uat:check
```

然后向用户一次性呈现：

`docs/phase-4-experience-and-operations-acceptance.md#consolidated-owner-checkpoint`

如果 owner/provider 上下文还没准备好，继续完成可自动化检查，保持真实外部 rows 为 `PENDING`，不要停在重复提问。
