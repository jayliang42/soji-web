---
phase: 03-launch-content-and-customer-policy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260726120000_launch_content_contract.sql
  - supabase/schema.sql
  - supabase/tests/database_access.sql
  - apps/web/src/lib/supabase/database.types.ts
  - packages/types/src/index.ts
  - apps/web/src/lib/content.ts
  - apps/web/src/lib/content-access.ts
  - apps/web/src/app/api/admin/content/route.ts
  - apps/web/src/components/admin-content-form.tsx
  - apps/web/src/components/admin-content-editor.tsx
  - apps/web/tests/content-access.test.ts
  - apps/web/tests/admin-content-route.test.ts
autonomous: true
requirements: [CONT-01]
user_setup: []
must_haves:
  truths:
    - "D-03 D-04 D-05: published content has explicit preview/cover-alt/tag metadata, and restricted full body text never reaches preview, locked, unavailable, metadata, or client output."
    - "D-22 D-23: publication validation is shared and fail-closed; live data remains authoritative while demo fixtures stay explicit demo-only."
    - "D-01 D-02: the contract can identify one substantive flagship item with an owned cover without treating hotlinked demo rows as launch-ready."
  artifacts:
    - path: "supabase/migrations/20260726120000_launch_content_contract.sql"
      provides: "Forward content metadata/RPC migration"
      contains: "preview_markdown"
    - path: "apps/web/src/lib/content-access.ts"
      provides: "Server-selectable preview/full/locked/unavailable body projection"
    - path: "apps/web/src/lib/supabase/database.types.ts"
      provides: "Generated content columns and RPC arguments"
  key_links:
    - from: "apps/web/src/app/api/admin/content/route.ts"
      to: "public.upsert_content_item"
      via: "strict validated cover alt, tags, preview, and revision payload"
      pattern: "p_preview_markdown"
    - from: "apps/web/src/lib/content.ts"
      to: "apps/web/src/lib/content-access.ts"
      via: "mapped ContentItem preview selected before Markdown render"
      pattern: "preview"
---

<objective>
Make preview copy, owned-cover metadata, tags, and revision-safe publication first-class database/application contracts so later reader UI can prove real content states without leaking the complete restricted body.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-launch-content-and-customer-policy/03-CONTEXT.md
@.planning/phases/03-launch-content-and-customer-policy/03-RESEARCH.md
@.planning/phases/03-launch-content-and-customer-policy/03-UI-SPEC.md
@.planning/phases/03-launch-content-and-customer-policy/03-VALIDATION.md
@.planning/phases/03-launch-content-and-customer-policy/03-PATTERNS.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS 4.0.3 L1 — V4, V5, V8, V13, V14"
high_severity_gate: "STOP before commit if restricted-body sentinels leak or the migration/RPC/grant contract is not green."
threats:
  - threat_id: T-03-01
    category: Information Disclosure
    severity: HIGH
    component: "Restricted content projection"
    disposition: mitigate
    mitigation_plan: "Select explicit preview/full content on the server and assert the full-body sentinel is absent from every unauthorized result."
  - threat_id: T-03-02
    category: Tampering
    severity: HIGH
    component: "Content publication RPC and optimistic concurrency"
    disposition: mitigate
    mitigation_plan: "One forward migration, role-checked security-definer RPC, expected revision, strict arguments, RLS/grant regression, generated types."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED database and projection contracts</name>
  <files>supabase/tests/database_access.sql, apps/web/tests/content-access.test.ts, apps/web/tests/admin-content-route.test.ts</files>
  <read_first>supabase/tests/database_access.sql, supabase/migrations/20260714020000_content_optimistic_concurrency.sql, supabase/schema.sql, apps/web/src/lib/content-access.ts, apps/web/src/app/api/admin/content/route.ts, .planning/phases/03-launch-content-and-customer-policy/03-CONTEXT.md</read_first>
  <action>Add focused pgTAP assertions for `preview_markdown`, `cover_image_alt`, `tags`, new `upsert_content_item` arguments, revision increments, publish-time preservation, role grants, and rollback on invalid access rules. Extend Vitest fixtures with a public preview and a unique `PRIVATE PHASE3 BODY` sentinel; assert full returns body, preview returns explicit preview, locked returns null, unavailable returns only public preview/summary, and Admin rejects missing preview/alt/tags for a restricted published item. Run only these named tests for the RED observation and require failures that name the missing columns/fields rather than syntax, connection, or fixture errors.</action>
  <verify>
    <automated>corepack pnpm --filter @soji/web test -- tests/content-access.test.ts tests/admin-content-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Targeted Web tests fail only on the missing preview/cover-alt/tag behavior before implementation.
    - pgTAP additions name every new column/RPC/security invariant and retain existing test plan validity.
    - The sentinel cannot satisfy preview assertions through summary fallback.
  </acceptance_criteria>
  <done>The RED contract precisely encodes the Phase 3 content boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the forward content contract and strict Admin write</name>
  <files>supabase/migrations/20260726120000_launch_content_contract.sql, supabase/schema.sql, packages/types/src/index.ts, apps/web/src/lib/content.ts, apps/web/src/lib/content-access.ts, apps/web/src/app/api/admin/content/route.ts, apps/web/src/components/admin-content-form.tsx, apps/web/src/components/admin-content-editor.tsx, supabase/tests/database_access.sql, apps/web/tests/content-access.test.ts, apps/web/tests/admin-content-route.test.ts</files>
  <read_first>supabase/schema.sql, supabase/tests/database_access.sql, apps/web/src/lib/content.ts, apps/web/src/lib/content-access.ts, apps/web/src/app/api/admin/content/route.ts, apps/web/src/components/admin-content-form.tsx, apps/web/src/components/admin-content-editor.tsx, packages/types/src/index.ts, .planning/phases/03-launch-content-and-customer-policy/03-RESEARCH.md</read_first>
  <action>Create immutable migration `20260726120000_launch_content_contract.sql` and exact declarative mirror adding non-null `preview_markdown`, `cover_image_alt`, and `tags`; replace `upsert_content_item` with the expanded role-checked, revision-safe signature and preserve existing published timestamps. Add `preview`, `coverImageAlt`, and mapped live tags to `ContentItem`. Make `getVisibleContentBody` return explicit preview for `preview` and `unavailable`, complete body only for `full`, and null for `locked`. Extend strict Admin zod/RPC/form/editor payloads with preview (20–20,000 characters for restricted published rows), cover alt (8–300 when cover exists), normalized unique tags (max 8, each 2–40), and root-relative owned cover support; keep drafts editable, retain conflict responses, and reject public launch publication that lacks body/cover metadata. Preserve safe Markdown and never introduce raw HTML preview delimiters.</action>
  <verify>
    <automated>corepack pnpm --filter @soji/web test -- tests/content-access.test.ts tests/admin-content-route.test.ts &amp;&amp; corepack pnpm db:schema:check</automated>
  </verify>
  <acceptance_criteria>
    - Focused Web tests and declarative schema parity exit 0.
    - The new RPC remains editor/Admin only and update/delete revision behavior is unchanged.
    - `PRIVATE PHASE3 BODY` is returned only for `full`.
    - Live content maps database tags instead of replacing them with `[]`.
    - No prior migration is edited.
  </acceptance_criteria>
  <done>The database, server, and Admin share one explicit content publication contract.</done>
</task>

<task type="auto">
  <name>Task 3: Apply local schema and generate exact database types</name>
  <files>apps/web/src/lib/supabase/database.types.ts</files>
  <read_first>scripts/sync-supabase-types.mjs, package.json, apps/web/src/lib/supabase/database.types.ts, supabase/migrations/20260726120000_launch_content_contract.sql</read_first>
  <action>Apply/reapply the local Supabase migrations through `20260726120000`, run the focused pgTAP contract and full database schema check, then generate `database.types.ts` with the repository script. Do not hand-edit generated output and do not push the migration to production in this autonomous plan.</action>
  <verify>
    <automated>corepack pnpm db:types &amp;&amp; corepack pnpm db:types:check &amp;&amp; corepack pnpm --filter @soji/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Generated types contain all three columns and expanded RPC arguments.
    - Type parity and Web typecheck exit 0.
    - Local migration history ends at or after `20260726120000`; production is unchanged.
  </acceptance_criteria>
  <done>Schema and application contracts are synchronized for the reader plan.</done>
</task>

</tasks>

<verification>
- [ ] Focused content/Admin tests, pgTAP, schema parity, generated-type parity, and Web typecheck pass.
- [ ] Unauthorized modes never receive the complete body sentinel.
- [ ] Every HIGH threat is mitigated and no production schema push occurred.
</verification>

<success_criteria>
- CONT-01 has a first-class preview/full data contract.
- Editors can author real launch metadata without weakening revision or role boundaries.
- Later plans can render a meaningful preview from server-selected text.
</success_criteria>

<output>
Create `.planning/phases/03-launch-content-and-customer-policy/03-01-SUMMARY.md`.
</output>
