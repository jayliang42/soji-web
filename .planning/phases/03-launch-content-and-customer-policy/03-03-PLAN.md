---
phase: 03-launch-content-and-customer-policy
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/launch-inputs.ts
  - apps/web/src/lib/office-hours-presentation.ts
  - apps/web/src/app/api/admin/office-hours/route.ts
  - apps/web/src/components/admin-office-hours-editor.tsx
  - apps/web/src/app/office-hours/page.tsx
  - apps/web/tests/launch-inputs.test.ts
  - apps/web/tests/office-hours-presentation.test.ts
  - apps/web/tests/admin-office-hours-route.test.ts
  - apps/web/tests/office-hours-page.test.tsx
  - apps/web/tests/session-failure-pages.test.tsx
autonomous: true
requirements: [OPS-01]
user_setup: []
must_haves:
  truths:
    - "D-08 D-10 D-11 D-12: signup/replay targets are HTTPS, non-placeholder, server-gated, absent from unauthorized/error markup, and never fabricated by Codex."
    - "D-09: upcoming, replay-pending, and replay-ready states select one truthful action from an injected clock."
    - "D-24 D-25: pure projection, route, and rendered-HTML tests prove access and target non-disclosure."
  artifacts:
    - path: "apps/web/src/lib/launch-inputs.ts"
      provides: "Reusable fail-closed public destination validator"
    - path: "apps/web/src/lib/office-hours-presentation.ts"
      provides: "Server-only lifecycle/access projection with optional authorized href"
    - path: "apps/web/src/app/office-hours/page.tsx"
      provides: "Upcoming and replay reader sections"
  key_links:
    - from: "api/admin/office-hours/route.ts"
      to: "launch-inputs.ts"
      via: "same URL validator used before persistence"
      pattern: "validateOfficeHourDestination"
    - from: "office-hours/page.tsx"
      to: "office-hours-presentation.ts"
      via: "page renders only projected actions"
      pattern: "buildOfficeHourPresentation"
---

<objective>
Make Office Hours destinations production-valid and visible only through a lifecycle-aware, entitlement-verified server projection.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-launch-content-and-customer-policy/03-CONTEXT.md
@.planning/phases/03-launch-content-and-customer-policy/03-RESEARCH.md
@.planning/phases/03-launch-content-and-customer-policy/03-UI-SPEC.md
@.planning/phases/03-launch-content-and-customer-policy/03-PATTERNS.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS 4.0.3 L1 — V4, V5, V13, V14"
high_severity_gate: "STOP if an unauthorized/error view includes either target or unsafe URLs pass the shared validator."
threats:
  - threat_id: T-03-04
    category: Information Disclosure
    severity: HIGH
    component: "Protected Office Hours destinations"
    disposition: mitigate
    mitigation_plan: "Server projection omits href until current entitlement and lifecycle checks pass; HTML sentinel assertions."
  - threat_id: T-03-05
    category: Unvalidated Redirect / Spoofing
    severity: HIGH
    component: "Admin-supplied external destinations"
    disposition: mitigate
    mitigation_plan: "HTTPS-only parsed URL, no credentials/loopback/private/local/example host, stable validation errors, noreferrer/noopener."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize launch URL validation at the Admin boundary</name>
  <files>apps/web/src/lib/launch-inputs.ts, apps/web/src/app/api/admin/office-hours/route.ts, apps/web/src/components/admin-office-hours-editor.tsx, apps/web/tests/launch-inputs.test.ts, apps/web/tests/admin-office-hours-route.test.ts</files>
  <read_first>apps/web/src/app/api/admin/office-hours/route.ts, apps/web/src/components/admin-office-hours-editor.tsx, apps/web/tests/admin-office-hours-route.test.ts, apps/web/src/lib/navigation.ts, apps/web/src/lib/env.ts, .planning/phases/03-launch-content-and-customer-policy/03-RESEARCH.md</read_first>
  <action>Write RED table tests for valid HTTPS provider paths/query, `http:`, credentials, `example.com/.org/.net` and subdomains, localhost/loopback/private-IP literals, `.local`, malformed, and overlong URLs. Implement `validateOfficeHourDestination` in `launch-inputs.ts` returning stable reason codes without echoing the value. Replace the local route schema refinement with the shared validator, require signup on create/update, permit empty replay, and map invalid fields to actionable 400 responses. Add equivalent client field messages while preserving form values, revision conflicts, and focus on the first invalid field.</action>
  <verify>
    <automated>corepack pnpm --filter @soji/web test -- tests/launch-inputs.test.ts tests/admin-office-hours-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every unsafe/placeholder class is rejected and valid HTTPS URLs with paths/query are accepted.
    - Error payloads contain stable reasons and no submitted URL.
    - Editor/Admin role and optimistic-concurrency tests remain green.
    - No `http:` target can be newly persisted.
  </acceptance_criteria>
  <done>One validator owns Office Hours production target truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project lifecycle and access before rendering actions</name>
  <files>apps/web/src/lib/office-hours-presentation.ts, apps/web/src/app/office-hours/page.tsx, apps/web/tests/office-hours-presentation.test.ts, apps/web/tests/office-hours-page.test.tsx, apps/web/tests/session-failure-pages.test.tsx</files>
  <read_first>apps/web/src/app/office-hours/page.tsx, apps/web/src/lib/office-hours.ts, apps/web/tests/session-failure-pages.test.tsx, packages/domain/src/entitlements.ts, .planning/phases/03-launch-content-and-customer-policy/03-UI-SPEC.md</read_first>
  <action>Create a pure `buildOfficeHourPresentation(session, access, now)` that returns `upcoming`, `replay_pending`, `replay_ready`, or `unavailable`; only entitled/verified upcoming and replay-ready results may contain an action href. Write tests with unique private target sentinels for guest, wrong entitlement, entitled, access failure, invalid stored target, before/after start, missing replay, and timezone formatting. Refactor the page into `Upcoming` and `Replay library` sections, render exact UI-SPEC labels/actions, include timezone, and add safe new-tab attributes plus assistive new-tab text. Locked/error/static states receive no raw session object or target-bearing client prop.</action>
  <verify>
    <automated>corepack pnpm --filter @soji/web test -- tests/office-hours-presentation.test.ts tests/office-hours-page.test.tsx tests/session-failure-pages.test.tsx &amp;&amp; corepack pnpm --filter @soji/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Projection tests prove href is absent for guest, wrong entitlement, invalid target, replay pending, and access failure.
    - Rendered HTML never contains either private sentinel for unauthorized/error states.
    - Upcoming entitled state has `Reserve a seat`; completed entitled state has `Watch replay` or `Replay coming soon`.
    - External anchors include `_blank`, `noopener`, `noreferrer`, and accessible announcement.
  </acceptance_criteria>
  <done>Office Hours communicates lifecycle honestly and exposes targets only to eligible members.</done>
</task>

</tasks>

<verification>
- [ ] Shared validator, Admin route, projection, page, failure regression, and typecheck pass.
- [ ] Raw target sentinel is absent from every unauthorized/error static response.
- [ ] No owner/provider URL was invented or marked ready.
</verification>

<success_criteria>
- OPS-01 application behavior is complete and ready for exact owner-supplied destinations.
- Upcoming/replay states match the Phase 3 UI contract.
</success_criteria>

<output>
Create `.planning/phases/03-launch-content-and-customer-policy/03-03-SUMMARY.md`.
</output>
