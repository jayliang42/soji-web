---
phase: 01-production-identity-and-admin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/auth-redirect.ts
  - apps/web/src/lib/auth-recovery.ts
  - apps/web/src/lib/supabase/auth-errors.ts
  - apps/web/src/components/login-form.tsx
  - apps/web/src/app/login/page.tsx
  - apps/web/src/app/reset-password/page.tsx
  - apps/web/src/components/password-reset-form.tsx
  - apps/web/tests/auth-redirect.test.ts
  - apps/web/tests/auth-errors.test.ts
  - apps/web/tests/login-page.test.tsx
  - apps/web/tests/reset-password-page.test.tsx
  - apps/web/e2e/public-pages.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03 D-05: /login is a dedicated destination-aware editorial page with Google prominent and email sign-in/account creation fully available."
    - "D-04 D-15: email confirmation and Google OAuth both use the canonical configured origin and preserve only a validated local nextPath, defaulting to /account."
    - "D-06 D-07 D-08 D-09 D-10: signup confirmation, recovery, expired-link, and password-update states are explicit, privacy-safe, and never expose provider internals."
    - "A configured production browser origin cannot be replaced by a preview/proxy Host during signup, OAuth, or recovery."
  artifacts:
    - path: "apps/web/src/lib/auth-redirect.ts"
      provides: "Shared canonical Auth callback construction"
      exports:
        - "getAuthCallbackUrl"
    - path: "apps/web/src/lib/supabase/auth-errors.ts"
      provides: "Stable public Auth failure mapping"
      contains: "getPublicAuthFailureMessage"
    - path: "apps/web/src/components/login-form.tsx"
      provides: "Responsive Google/email auth and confirmation-state UI"
      contains: "Check your inbox"
    - path: "apps/web/tests/auth-redirect.test.ts"
      provides: "Open-redirect and canonical-origin regression coverage"
    - path: "apps/web/e2e/public-pages.spec.ts"
      provides: "Keyboard, responsive, and accessibility coverage for Auth surfaces"
  key_links:
    - from: "apps/web/src/components/login-form.tsx"
      to: "apps/web/src/lib/auth-redirect.ts"
      via: "signup emailRedirectTo and Google redirectTo"
      pattern: "getAuthCallbackUrl"
    - from: "apps/web/src/components/login-form.tsx"
      to: "apps/web/src/lib/supabase/auth-errors.ts"
      via: "stable operation-specific failure copy"
      pattern: "getPublicAuthFailureMessage"
    - from: "apps/web/src/lib/auth-redirect.ts"
      to: "apps/web/src/lib/navigation.ts"
      via: "safe local destination normalization"
      pattern: "getSafeNextPath"
---

<objective>
Close the remaining application-level Auth contracts and deliver the focused premium identity experience defined by the Phase 1 context and UI specification.

Purpose: A production confirmation or OAuth round trip must return to the intended safe Soji page, while every visible Auth outcome stays clear, accessible, and free of provider internals.
Output: Shared redirect/error helpers, refined login/reset UI, and unit/browser regression coverage.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-production-identity-and-admin/01-CONTEXT.md
@.planning/phases/01-production-identity-and-admin/01-RESEARCH.md
@.planning/phases/01-production-identity-and-admin/01-UI-SPEC.md
@.planning/phases/01-production-identity-and-admin/01-PATTERNS.md
@.planning/phases/01-production-identity-and-admin/01-VALIDATION.md
</context>

<threat_model>
trust_boundaries:
  - "Untrusted browser/query-string input → canonical application callback URL"
  - "Supabase/Google provider errors → customer-visible copy"
  - "One-time email/OAuth callback → server-side session and profile bootstrap"
threats:
  - threat_id: T-01-01
    category: Spoofing
    component: "Auth redirect destination"
    disposition: mitigate
    mitigation_plan: "Normalize every nextPath with getSafeNextPath and construct callbacks from NEXT_PUBLIC_SITE_URL; cover external, protocol-relative, malformed, and proxy-host inputs."
  - threat_id: T-01-02
    category: Information Disclosure
    component: "Login and signup error rendering"
    disposition: mitigate
    mitigation_plan: "Map failures to stable operation-specific Soji copy; never render Supabase or Google error.message."
  - threat_id: T-01-03
    category: Tampering
    component: "Signup/OAuth callback origin"
    disposition: mitigate
    mitigation_plan: "Use one tested getAuthCallbackUrl helper for signUp emailRedirectTo and Google redirectTo; production rejects missing/invalid canonical origins."
  - threat_id: T-01-04
    category: Denial of Service
    component: "Repeated Auth actions"
    disposition: mitigate
    mitigation_plan: "Allow only one pending operation, disable competing actions, and preserve retryable UI without automatic loops."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical Auth redirect and stable failure contracts</name>
  <files>apps/web/src/lib/auth-redirect.ts, apps/web/src/lib/auth-recovery.ts, apps/web/src/lib/supabase/auth-errors.ts, apps/web/tests/auth-redirect.test.ts, apps/web/tests/auth-errors.test.ts</files>
  <read_first>apps/web/src/lib/navigation.ts, apps/web/src/lib/env.ts, apps/web/src/lib/auth-recovery.ts, apps/web/src/lib/supabase/auth-errors.ts, apps/web/tests/navigation.test.ts, apps/web/tests/env.test.ts, apps/web/tests/auth-recovery.test.ts, .planning/phases/01-production-identity-and-admin/01-CONTEXT.md, .planning/phases/01-production-identity-and-admin/01-RESEARCH.md</read_first>
  <action>Create `getAuthCallbackUrl(siteOrigin: string, nextPath: string, options?: { flow?: "recovery" }): string` in `auth-redirect.ts`. It must call `getSafeNextPath`, build `/auth/callback`, set an encoded `next` query value, and set `flow=recovery` only for recovery. Refactor `getPasswordRecoveryRedirectUrl` to delegate to this helper without changing its public output. Extend `supabase/auth-errors.ts` with `getPublicAuthFailureMessage(operation: "email_sign_in" | "email_sign_up" | "google"): string`; return exactly the UI-SPEC messages and never interpolate `error.message`. Add tests for `/account`, `/admin?view=users`, protected Library paths, `https://attacker.example`, `//attacker.example`, malformed paths, canonical production origins, recovery flow, and provider-detail suppression.</action>
  <verify>corepack pnpm --filter @soji/web test -- tests/auth-redirect.test.ts tests/auth-errors.test.ts tests/auth-recovery.test.ts tests/navigation.test.ts tests/env.test.ts</verify>
  <acceptance_criteria>
    - `apps/web/src/lib/auth-redirect.ts` exports `getAuthCallbackUrl` and imports `getSafeNextPath`.
    - `getAuthCallbackUrl("https://soji.example", "/admin?view=users")` produces a URL whose origin is `https://soji.example`, pathname is `/auth/callback`, and decoded `next` is `/admin?view=users`.
    - External and protocol-relative next values resolve to `/account`.
    - `getPublicAuthFailureMessage` returns no supplied provider message and exposes no account-existence distinction.
    - The focused Vitest command exits 0.
  </acceptance_criteria>
  <done>Canonical callback generation and stable customer-facing failure mapping are shared, tested, and free of raw provider output.</done>
</task>

<task type="auto">
  <name>Task 2: Refine the login, confirmation, and reset interaction states</name>
  <files>apps/web/src/components/login-form.tsx, apps/web/src/app/login/page.tsx, apps/web/src/app/reset-password/page.tsx, apps/web/src/components/password-reset-form.tsx</files>
  <read_first>apps/web/src/components/login-form.tsx, apps/web/src/app/login/page.tsx, apps/web/src/app/reset-password/page.tsx, apps/web/src/components/password-reset-form.tsx, apps/web/src/components/profile-setup-retry.tsx, apps/web/src/components/data-state.tsx, apps/web/src/lib/login-copy.ts, apps/web/src/lib/auth-redirect.ts, apps/web/src/lib/supabase/auth-errors.ts, .planning/phases/01-production-identity-and-admin/01-UI-SPEC.md</read_first>
  <action>Implement the exact UI-SPEC control order: destination-aware `h2`, full-width `Continue with Google`, divider text `or continue with email`, two-button `aria-pressed` mode control, labeled email/password fields, full-width mode-specific email action, recovery action, and one status region. Pass `options.emailRedirectTo = getAuthCallbackUrl(siteUrl, safeNextPath)` to `signUp`; use the same helper for Google `redirectTo`. Track the active operation so pending labels are exactly `Signing in…`, `Creating account…`, `Opening Google…`, or `Sending reset link…`, with the form marked `aria-busy`. Replace the credential controls after signup-without-session with the UI-SPEC `Check your inbox` state, `Use a different email`, and `Return to sign in`. Render only stable failure copy. Update reset success/invalid-link copy to the exact UI-SPEC contract while retaining recovery-session gating and no raw provider details. Keep the dedicated page layout, existing `SectionShell`, 44px minimum targets, 24px mobile/32px desktop card spacing, and no new component library.</action>
  <verify>corepack pnpm --filter @soji/web test -- tests/login-page.test.tsx tests/auth-recovery.test.ts tests/reset-password-page.test.tsx && corepack pnpm --filter @soji/web typecheck</verify>
  <acceptance_criteria>
    - `login-form.tsx` contains `Continue with Google`, `or continue with email`, `Sign in with email`, `Check your inbox`, `Use a different email`, and `Return to sign in`.
    - `signUp` receives `emailRedirectTo` from `getAuthCallbackUrl`; Google receives `redirectTo` from the same helper.
    - No catch branch renders `error.message` or another provider-supplied message.
    - Pending state disables Google, mode, email, password, recovery, and submit controls and exposes an operation-specific label.
    - Email confirmation does not call `/api/auth/bootstrap` and does not redirect until the confirmation callback succeeds.
    - The focused Vitest and typecheck commands exit 0.
  </acceptance_criteria>
  <done>Auth surfaces implement the locked hierarchy and every signup/recovery outcome has a clear, privacy-safe next action.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the identity experience with component and browser tests</name>
  <files>apps/web/tests/login-page.test.tsx, apps/web/tests/reset-password-page.test.tsx, apps/web/e2e/public-pages.spec.ts</files>
  <read_first>apps/web/tests/login-page.test.tsx, apps/web/tests/reset-password-page.test.tsx, apps/web/e2e/public-pages.spec.ts, apps/web/src/components/login-form.tsx, apps/web/src/app/login/page.tsx, apps/web/src/components/password-reset-form.tsx, .planning/phases/01-production-identity-and-admin/01-UI-SPEC.md, .planning/phases/01-production-identity-and-admin/01-VALIDATION.md</read_first>
  <action>Update server-rendered tests for the exact hierarchy/copy and add browser coverage for Google-before-email ordering, explicit mode switching, Enter submission, recovery acknowledgement, callback failure recovery, and reset-link invalid state. Add desktop and mobile assertions that the primary task is visible, controls are at least 44px high, and the page has no horizontal overflow at 375px. Run axe on `/login` and `/reset-password`; keep serious/critical violations blocking. Do not call Google or send email in deterministic tests. If a confirmation state needs browser coverage, intercept only the Supabase Auth request at the test boundary and return the no-session signup shape—do not add production test hooks.</action>
  <verify>corepack pnpm --filter @soji/web test && corepack pnpm --filter @soji/web test:e2e -- --grep "login|authentication|password|accessibility"</verify>
  <acceptance_criteria>
    - Unit/component tests assert the exact stable Auth and reset copy from `01-UI-SPEC.md`.
    - Playwright asserts `Continue with Google` precedes the email divider and email fields.
    - Playwright covers desktop and mobile without horizontal overflow and with minimum 44px action height.
    - Axe reports no serious or critical violation on `/login` and `/reset-password`.
    - The full Web Vitest command and focused Playwright command exit 0.
  </acceptance_criteria>
  <done>The refined identity flow is protected by deterministic contract, keyboard, responsive, and accessibility tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @soji/web test` exits 0.
- [ ] `corepack pnpm --filter @soji/web test:e2e -- --grep "login|authentication|password|accessibility"` exits 0.
- [ ] `corepack pnpm --filter @soji/web typecheck` exits 0.
- [ ] `corepack pnpm --filter @soji/web lint` exits 0.
- [ ] No changed Auth component renders provider `error.message`.
- [ ] Signup, OAuth, and recovery callback tests prove canonical origin plus safe nextPath behavior.
</verification>

<success_criteria>
- All three tasks and their acceptance criteria pass.
- Email confirmation and Google OAuth preserve a safe intended destination.
- Login/signup/recovery UI follows `01-UI-SPEC.md` on desktop/mobile and remains keyboard/axe compliant.
- Provider internals and account-existence signals are absent from customer-facing Auth errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-identity-and-admin/01-01-SUMMARY.md`.
</output>
