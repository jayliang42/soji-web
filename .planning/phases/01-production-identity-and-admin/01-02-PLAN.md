---
phase: 01-production-identity-and-admin
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-phase1-uat-evidence.mjs
  - scripts/check-phase1-uat-evidence.test.mjs
  - package.json
  - docs/phase-1-production-identity-uat.md
  - docs/launch-checklist.md
  - .planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md
autonomous: true
requirements:
  - INFRA-01
  - AUTH-01
  - AUTH-02
  - ADMIN-01
user_setup: []
must_haves:
  truths:
    - "D-11 D-12 D-13 D-14 D-15: Phase 1 evidence distinguishes canonical production OAuth/SMTP and Supabase readiness from local, preview, demo, and Stripe-dependent release readiness."
    - "D-16 D-17 D-18 D-19 D-20: Existing first-Admin evidence is preserved, later role changes are verified through the Users workspace, final-Admin protection is tested, and every role-appropriate workspace is listed."
    - "The UAT artifact cannot pass its ready-mode validator while any required scenario is pending or while it contains likely secrets, tokens, cookies, passwords, or raw test email addresses."
  artifacts:
    - path: "docs/phase-1-production-identity-uat.md"
      provides: "Exact provider-backed Phase 1 operator runbook"
      min_lines: 100
    - path: ".planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md"
      provides: "Secret-free scenario outcomes for all four Phase 1 requirements"
      contains: "AUTH-01"
    - path: "scripts/check-phase1-uat-evidence.mjs"
      provides: "Deterministic structure, completeness, and secret-safety gate"
    - path: "scripts/check-phase1-uat-evidence.test.mjs"
      provides: "Validator regression tests"
  key_links:
    - from: "package.json"
      to: "scripts/check-phase1-uat-evidence.mjs"
      via: "phase1:uat:check and phase1:uat:ready scripts"
      pattern: "phase1:uat"
    - from: "docs/launch-checklist.md"
      to: "docs/phase-1-production-identity-uat.md"
      via: "Phase 1 Auth/Admin checklist link"
      pattern: "phase-1-production-identity-uat"
    - from: "docs/phase-1-production-identity-uat.md"
      to: ".planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md"
      via: "evidence recording instructions"
      pattern: "01-UAT-EVIDENCE"
---

<objective>
Make Phase 1 production evidence reproducible, secret-free, and mechanically checkable before any provider-backed actions occur.

Purpose: Automated tests prove contracts, but launch decisions need a durable record of real SMTP, Google, Supabase, and Admin outcomes without leaking operational secrets.
Output: A focused UAT runbook, pending evidence artifact, safety/completeness validator with tests, and launch-checklist integration.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-production-identity-and-admin/01-CONTEXT.md
@.planning/phases/01-production-identity-and-admin/01-RESEARCH.md
@.planning/phases/01-production-identity-and-admin/01-VALIDATION.md
@docs/launch-checklist.md
@supabase/README.md
</context>

<threat_model>
trust_boundaries:
  - "Production provider/dashboard output → repository evidence"
  - "Historical one-time Admin bootstrap → repeatable operating procedure"
  - "Release-wide readiness response → Phase 1 identity verdict"
threats:
  - threat_id: T-01-05
    category: Elevation of Privilege
    component: "First and later Admin procedures"
    disposition: mitigate
    mitigation_plan: "Document bootstrap as historical one-time evidence and require all later changes through the audited Users RPC, including final-Admin rejection."
  - threat_id: T-01-06
    category: Repudiation
    component: "Provider-backed UAT"
    disposition: mitigate
    mitigation_plan: "Require UTC date, scenario ID, redacted account label, expected/observed outcome, and PASS/FAIL status for each requirement."
  - threat_id: T-01-07
    category: Information Disclosure
    component: "UAT evidence Markdown"
    disposition: mitigate
    mitigation_plan: "Validator rejects likely secrets, tokens, cookies, passwords, whsec/service keys, authorization headers, and email-address patterns."
  - threat_id: T-01-08
    category: Tampering
    component: "Phase readiness interpretation"
    disposition: mitigate
    mitigation_plan: "Require named Supabase booleans and /api/me source; explicitly exclude overall Stripe-dependent ok from the Phase 1 verdict."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the secret-safe Phase 1 evidence validator</name>
  <files>scripts/check-phase1-uat-evidence.mjs, scripts/check-phase1-uat-evidence.test.mjs, package.json</files>
  <read_first>package.json, scripts/check-api-docs.mjs, scripts/check-doc-links.mjs, .planning/phases/01-production-identity-and-admin/01-VALIDATION.md, .planning/phases/01-production-identity-and-admin/01-RESEARCH.md</read_first>
  <action>Create an ESM Node validator with two modes: default structure/safety mode and `--ready` completeness mode. Parse the canonical evidence file path from a positional argument defaulting to `.planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md`. Require scenario IDs `INFRA-01-MIGRATIONS`, `INFRA-01-READINESS`, `AUTH-01-SIGNUP`, `AUTH-01-RECOVERY`, `AUTH-02-GOOGLE`, `ADMIN-01-BOOTSTRAP`, `ADMIN-01-ROLE-TRANSITION`, and `ADMIN-01-WORKSPACES`. Allow statuses only `PENDING`, `PASS`, `FAIL`, `BLOCKED`. Default mode accepts pending but rejects missing/duplicate IDs and likely secrets; ready mode exits nonzero unless every ID is `PASS`. Reject case-insensitive patterns for JWT-like bearer values, `service_role`, `SUPABASE_SERVICE_ROLE_KEY=`, `whsec_`, `sk_live_`, `password=`, cookie/authorization headers, URI userinfo, and ordinary email-address strings. Export pure parser/validator functions for Node tests. Add root scripts `phase1:uat:check`, `phase1:uat:ready`, and `test:uat`. Tests must cover a valid pending artifact, a ready artifact, missing/duplicate scenarios, invalid status, secret/token patterns, and email-address rejection.</action>
  <verify>corepack pnpm test:uat</verify>
  <acceptance_criteria>
    - `package.json` defines `phase1:uat:check`, `phase1:uat:ready`, and `test:uat`.
    - Default validation accepts all required IDs with `PENDING` and rejects every listed secret/email pattern.
    - `--ready` exits nonzero for any `PENDING`, `FAIL`, or `BLOCKED` row and exits 0 only when all eight IDs are `PASS`.
    - `corepack pnpm test:uat` exits 0 without network access.
  </acceptance_criteria>
  <done>UAT evidence has a deterministic structure, completeness, and secret-leak gate.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Phase 1 production identity/Admin runbook and pending evidence</name>
  <files>docs/phase-1-production-identity-uat.md, docs/launch-checklist.md, .planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md</files>
  <read_first>docs/launch-checklist.md, supabase/README.md, supabase/publisher-setup.sql, .planning/REQUIREMENTS.md, .planning/phases/01-production-identity-and-admin/01-CONTEXT.md, .planning/phases/01-production-identity-and-admin/01-RESEARCH.md, .planning/phases/01-production-identity-and-admin/01-VALIDATION.md</read_first>
  <action>Create `docs/phase-1-production-identity-uat.md` with prerequisites, redaction policy, exact commands/browser journeys, expected values, failure recovery, and evidence instructions for all eight validator scenario IDs. For readiness, require `demoModeDisabled`, `siteUrl`, `supabase`, `supabaseAdmin`, `supabasePublicOperational`, and `supabaseServiceRoleOperational` to be true, but state that Stripe fields may keep overall `/api/health/ready` at 503 until Phase 2. For first Admin, require historical `member + admin`, unchanged paid tier, and `first_admin_bootstrap`; prohibit rerunning bootstrap. For later roles, require Users-workspace grant/revoke, audit evidence, final-Admin 409, and role-appropriate workspace coverage. Create the canonical evidence table with all rows `PENDING` and no real email/domain/token values beyond public placeholder `https://<canonical-origin>`. Update launch checklist sections 3–5 to link the focused runbook/evidence, preserve already completed Supabase/Admin facts, and leave canonical OAuth/SMTP items unchecked until Plan 03 produces real PASS evidence.</action>
  <verify>corepack pnpm phase1:uat:check && corepack pnpm docs:check</verify>
  <acceptance_criteria>
    - The runbook contains all eight scenario IDs and exact expected outcomes.
    - The evidence artifact contains exactly eight unique required rows, initially `PENDING`.
    - The runbook explicitly says full readiness `ok` may remain false because Stripe is Phase 2 and names the six Phase 1 booleans.
    - The runbook says `publisher-setup.sql` is not rerun after first Admin exists and later changes use Admin Users.
    - `docs/launch-checklist.md` does not mark production-domain OAuth, custom SMTP, signup confirmation, or recovery complete without PASS evidence.
    - `corepack pnpm phase1:uat:check` and `corepack pnpm docs:check` exit 0.
  </acceptance_criteria>
  <done>Operators have a complete, safe Phase 1 procedure and a mechanically valid pending evidence record.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm test:uat` exits 0.
- [ ] `corepack pnpm phase1:uat:check` exits 0.
- [ ] `corepack pnpm phase1:uat:ready` exits nonzero while the artifact is pending.
- [ ] `corepack pnpm docs:check` exits 0.
- [ ] No evidence/runbook file contains an email address, token, secret value, cookie, or authorization header.
</verification>

<success_criteria>
- Validator and tests enforce all eight Phase 1 scenarios and secret-safety rules.
- Runbook steps cleanly separate repository checks, automatable provider checks, and unavoidable email/OAuth human actions.
- Launch checklist preserves truthful historical status and links the focused evidence.
- Pending evidence cannot be mistaken for Phase 1 completion.
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-identity-and-admin/01-02-SUMMARY.md`.
</output>
