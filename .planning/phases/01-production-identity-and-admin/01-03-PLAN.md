---
phase: 01-production-identity-and-admin
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md
  - docs/launch-checklist.md
autonomous: false
requirements:
  - INFRA-01
  - AUTH-01
  - AUTH-02
  - ADMIN-01
user_setup:
  - service: supabase-production
    why: "Migration parity, SMTP/Auth configuration, readiness, and role audit require the linked production project."
    dashboard_config:
      - task: "Provide access to the existing production Supabase project if the CLI/browser session is not already authenticated."
        location: "Supabase account/organization containing the Soji project"
        details: "Do not paste service-role keys or access tokens into Markdown or chat; use the existing authenticated CLI/browser or an approved secret input."
  - service: external-identity-uat
    why: "Email verification links and Google consent are human identity actions with no safe headless substitute."
    dashboard_config:
      - task: "Make two non-production UAT mailboxes and one Google UAT identity available when Codex reaches the authentication gate."
        location: "Two different mailbox providers and a Google account"
        details: "Evidence uses labels only; no addresses, passwords, one-time links, or tokens are stored."
must_haves:
  truths:
    - "D-11 D-12 D-13 D-14 D-15: canonical production signup, recovery, and Google journeys pass with two mailbox providers, Supabase session source, required readiness booleans, and secret-free dated evidence."
    - "D-16 D-17 D-18 D-19 D-20: historical first-Admin invariants, second-Admin grant/revoke, final-Admin rejection, audit rows, and role-appropriate workspace access pass in production."
    - "INFRA-01, AUTH-01, AUTH-02, and ADMIN-01 all have PASS evidence and `corepack pnpm phase1:uat:ready` exits 0."
  artifacts:
    - path: ".planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md"
      provides: "Completed redacted production UAT record"
      contains: "PASS"
    - path: "docs/launch-checklist.md"
      provides: "Truthful production identity/Admin completion status"
      contains: "Phase 1"
  key_links:
    - from: ".planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md"
      to: "scripts/check-phase1-uat-evidence.mjs"
      via: "ready-mode validation"
      pattern: "AUTH-02-GOOGLE"
    - from: "docs/launch-checklist.md"
      to: ".planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md"
      via: "completed Phase 1 evidence link"
      pattern: "01-UAT-EVIDENCE"
---

<objective>
Execute and record the real production Supabase, SMTP, Google OAuth, password recovery, and Admin acceptance journeys required to close Phase 1.

Purpose: Repository tests cannot prove external provider delivery, canonical callbacks, production migration parity, or the live role audit. This plan gathers that authoritative evidence without exposing credentials.
Output: All eight UAT scenarios marked PASS, truthful launch-checklist status, and a passing ready-mode evidence gate.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-production-identity-and-admin/01-CONTEXT.md
@.planning/phases/01-production-identity-and-admin/01-RESEARCH.md
@.planning/phases/01-production-identity-and-admin/01-UI-SPEC.md
@.planning/phases/01-production-identity-and-admin/01-VALIDATION.md
@.planning/phases/01-production-identity-and-admin/01-01-SUMMARY.md
@.planning/phases/01-production-identity-and-admin/01-02-SUMMARY.md
@docs/phase-1-production-identity-uat.md
</context>

<threat_model>
trust_boundaries:
  - "Local authenticated CLI/browser → production Supabase and canonical Web deployment"
  - "External mailbox/Google identity → one-time Auth action"
  - "Production role operations → redacted repository evidence"
threats:
  - threat_id: T-01-09
    category: Tampering
    component: "Production migration state"
    disposition: mitigate
    mitigation_plan: "Run migration list and db push --dry-run; apply only reviewed forward migrations, never include seed without separate catalog review, and record version parity."
  - threat_id: T-01-10
    category: Spoofing
    component: "Provider-backed test identity"
    disposition: mitigate
    mitigation_plan: "Use labeled UAT accounts, verify /api/me source and intended destination after callback, and never infer success from redirect alone."
  - threat_id: T-01-11
    category: Elevation of Privilege
    component: "Admin role transitions"
    disposition: mitigate
    mitigation_plan: "Perform changes only through Admin Users, verify audit rows and final-Admin rejection, then return the temporary account to its intended role."
  - threat_id: T-01-12
    category: Information Disclosure
    component: "Captured UAT results"
    disposition: mitigate
    mitigation_plan: "Record labels/booleans/statuses only and run both evidence safety and ready validators before commit."
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Prove production migration parity and Supabase identity readiness</name>
  <files>.planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md</files>
  <read_first>docs/phase-1-production-identity-uat.md, docs/launch-checklist.md, supabase/README.md, supabase/migrations/20260714110000_audited_first_admin_bootstrap.sql, .planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md, apps/web/src/app/api/health/ready/route.ts, apps/web/src/app/api/me/route.ts</read_first>
  <action>Use the existing authenticated Supabase CLI/project link; never print secrets. Run production `supabase migration list` and `supabase db push --dry-run`. If forward migrations are pending, inspect the exact versions, confirm no destructive or seed operation is included, then run `supabase db push`; do not use `--include-seed`. Query the canonical deployment's `/api/health/ready` and record only the booleans for `demoModeDisabled`, `siteUrl`, `supabase`, `supabaseAdmin`, `supabasePublicOperational`, and `supabaseServiceRoleOperational`. Query `/api/me` and require `source: "supabase"` (a signed-out `user: null` is acceptable for this preflight). Update only `INFRA-01-MIGRATIONS` and `INFRA-01-READINESS` with UTC date, redacted environment label, observed boolean/version summary, and PASS/FAIL/BLOCKED.</action>
  <verify>corepack pnpm phase1:uat:check</verify>
  <acceptance_criteria>
    - Local and production migration version lists match after any required forward push.
    - No deployed migration file was rewritten and no production seed was applied.
    - All six named Phase 1 readiness booleans are true.
    - `/api/me` reports `source: "supabase"`.
    - The two INFRA rows are `PASS`, contain no credentials/addresses/tokens, and `corepack pnpm phase1:uat:check` exits 0.
  </acceptance_criteria>
  <done>INFRA-01 has authoritative migration and live public/service-role identity evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Start canonical email and Google authentication journeys</name>
  <files>.planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md</files>
  <read_first>docs/phase-1-production-identity-uat.md, apps/web/src/components/login-form.tsx, apps/web/src/app/auth/callback/route.ts, apps/web/src/lib/auth-recovery.ts, .planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md</read_first>
  <action>Using the canonical production site in the controlled browser, initiate signup for two labeled UAT identities on different mailbox providers, initiate one password-recovery request, and open the Google authentication journey from a protected destination. Automate navigation, form filling, non-sensitive observations, and post-callback checks. Stop dynamically at the authentication gate before requesting mailbox access, clicking one-time email links, entering credentials, or approving Google consent. Do not use preview/local origins as evidence and do not place account addresses in the evidence artifact.</action>
  <verify>The canonical site has sent both confirmation emails and one recovery email, and the browser is positioned at each unavoidable external identity action without a leaked credential.</verify>
  <acceptance_criteria>
    - Both signup requests show the privacy-safe `Check your inbox` state on the canonical origin.
    - Recovery shows the uniform privacy-safe acknowledgement.
    - Google authentication was initiated with a callback targeting the canonical origin and a validated local destination.
    - No password, email address, OAuth code, one-time link, token, cookie, or authorization value is written to tracked files or tool commentary.
  </acceptance_criteria>
  <done>All automatable setup for the external identity journeys is complete and only human identity authorization remains.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Complete the external mailbox verification/recovery links and Google account consent that Codex initiated.</action>
  <instructions>Codex has opened the canonical Soji journeys and completed every non-sensitive automatable step. In the controlled browser/inboxes, open the newest two signup confirmation links, open the newest password-recovery link and approve the Google sign-in consent. Do not paste links, passwords, codes, cookies, or tokens into chat. Return only the signal `done` after the provider actions finish.</instructions>
  <verification>Codex will verify canonical destinations, `/api/me` Supabase source, new-password sign-in, and evidence statuses without reading or storing credentials.</verification>
  <resume-signal>Type `done` when the external identity actions are complete.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify Auth outcomes and Admin audit invariants, then close evidence</name>
  <files>.planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md, docs/launch-checklist.md</files>
  <read_first>docs/phase-1-production-identity-uat.md, docs/launch-checklist.md, .planning/phases/01-production-identity-and-admin/01-UAT-EVIDENCE.md, apps/web/src/app/admin/page.tsx, apps/web/src/components/admin-users.tsx, apps/web/src/app/api/admin/users/roles/route.ts, supabase/publisher-setup.sql, supabase/migrations/20260714110000_audited_first_admin_bootstrap.sql</read_first>
  <action>Verify both confirmed signup identities can sign in; verify the recovery identity can update its password and sign in with the new password; verify Google returns to the protected intended destination and `/api/me` reports `source: "supabase"`. In production, inspect the historical first-Admin result without rerunning bootstrap: member+admin roles, unchanged paid tier, and `first_admin_bootstrap` audit source. Through `/admin?view=users`, grant a labeled second UAT account Admin, verify its Admin/billing/users access, verify the original final-Admin cannot be demoted while it is the only Admin in the dedicated rejection setup, verify audit rows for later changes, and return the temporary account to its intended non-Admin role. Verify editor/member denial and every Admin workspace from the runbook. Mark the six remaining rows PASS only from observed results. Run safety and ready validators, then update launch checklist Phase 1/Auth/Admin status and link the completed evidence.</action>
  <verify>corepack pnpm phase1:uat:check && corepack pnpm phase1:uat:ready && corepack pnpm docs:check</verify>
  <acceptance_criteria>
    - `AUTH-01-SIGNUP`, `AUTH-01-RECOVERY`, and `AUTH-02-GOOGLE` are PASS with canonical destinations and no sensitive values.
    - `ADMIN-01-BOOTSTRAP`, `ADMIN-01-ROLE-TRANSITION`, and `ADMIN-01-WORKSPACES` are PASS with audit/final-Admin/role-boundary observations.
    - Exactly eight evidence scenarios are PASS and none are PENDING, FAIL, or BLOCKED.
    - `corepack pnpm phase1:uat:check`, `corepack pnpm phase1:uat:ready`, and `corepack pnpm docs:check` all exit 0.
    - Launch checklist marks only the actually observed Phase 1 items complete and retains truthful pending Stripe/SMTP policy items outside the evidence scope.
  </acceptance_criteria>
  <done>All four Phase 1 requirements have real production evidence and the ready gate passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Production/local migration versions match and no unreviewed seed/destructive change was applied.
- [ ] Both signup confirmations, password recovery/new-password sign-in, and Google canonical callback pass.
- [ ] Required Supabase readiness booleans are true and `/api/me` source is Supabase.
- [ ] First-Admin history, second-Admin grant/revoke, final-Admin rejection, audit rows, and workspace boundaries pass.
- [ ] `corepack pnpm phase1:uat:check` exits 0.
- [ ] `corepack pnpm phase1:uat:ready` exits 0.
- [ ] `corepack pnpm docs:check` exits 0.
- [ ] Evidence contains no account address, credential, token, cookie, OAuth code, one-time link, or secret.
</verification>

<success_criteria>
- INFRA-01, AUTH-01, AUTH-02, and ADMIN-01 each have authoritative PASS evidence.
- A real canonical-domain user can confirm email, sign in, recover/change password, and sign in again.
- A real Google user returns through the canonical callback to the intended safe page.
- Production Supabase public/service-role readiness and Admin audit/control-plane behavior are proven.
- Phase 1 evidence and launch status remain secret-free and mechanically validated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-identity-and-admin/01-03-SUMMARY.md`.
</output>
