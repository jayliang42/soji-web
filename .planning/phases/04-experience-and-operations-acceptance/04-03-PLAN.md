---
phase: 04-experience-and-operations-acceptance
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/cron-auth.ts
  - apps/web/src/app/api/cron/product-asset-cleanup/route.ts
  - apps/web/src/lib/product-asset-cleanup.ts
  - apps/web/tests/cron-auth.test.ts
  - apps/web/tests/product-asset-cleanup-cron-route.test.ts
  - apps/web/tests/product-asset-cleanup.test.ts
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "D-16: Admin and scheduled cleanup call the same lease-based executor and never accept a caller storage path."
    - "D-17 D-18: only an exact Bearer secret of at least 32 trimmed characters is authorized; responses contain stable aggregate outcomes only."
    - "D-19 D-20: partial failure remains durable and retryable, while claim/attempt-record failure cannot report scheduler success."
  artifacts:
    - path: "apps/web/src/app/api/cron/product-asset-cleanup/route.ts"
      provides: "Authenticated aggregate-only scheduled cleanup boundary"
    - path: "apps/web/tests/product-asset-cleanup-cron-route.test.ts"
      provides: "Auth, executor reuse, redaction, and result mapping proof"
  key_links:
    - from: "cron route"
      to: "processDueProductAssetCleanupJobs"
      via: "fixed scheduled-cleanup actor and limit"
      pattern: "scheduled-cleanup"
---

<objective>
Prove scheduled private-file cleanup is exactly authenticated, shares the lease executor,
returns aggregate truth only, and preserves retryable failure state.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-experience-and-operations-acceptance/04-CONTEXT.md
@.planning/phases/04-experience-and-operations-acceptance/04-RESEARCH.md
@.planning/phases/04-experience-and-operations-acceptance/04-VALIDATION.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS L1 — V2 Authentication, V4 Access Control, V8 Data Protection, V14 Configuration"
high_severity_gate: "STOP if malformed/incorrect auth differs observably, a caller chooses storage paths, or a response leaks paths/errors/service details."
threats:
  - threat_id: T-04-05
    category: Broken Authentication / Information Disclosure
    severity: HIGH
    component: "Scheduled cleanup route"
    disposition: mitigate
    mitigation_plan: "Exact timing-safe Bearer comparison, stable denial, fixed executor inputs, aggregate response projection."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock exact cron authorization and aggregate route output</name>
  <files>apps/web/src/lib/cron-auth.ts, apps/web/src/app/api/cron/product-asset-cleanup/route.ts, apps/web/tests/cron-auth.test.ts, apps/web/tests/product-asset-cleanup-cron-route.test.ts</files>
  <read_first>apps/web/src/lib/cron-auth.ts, apps/web/src/app/api/cron/product-asset-cleanup/route.ts, apps/web/src/lib/env.ts, apps/web/tests/cron-auth.test.ts, apps/web/tests/product-asset-cleanup-cron-route.test.ts, apps/web/src/app/api/admin/product-assets/cleanup/route.ts</read_first>
  <action>Add RED cases for missing, whitespace-only, under-32-character, malformed scheme/casing/spacing, prefix/suffix, and incorrect secrets; all return the same 401 `{ ok: false, reason: "cron_unauthorized" }` with `Cache-Control: no-store`. Retain a timing-safe equal-length comparison for exact `Bearer ${trimmedSecret}`. Assert the route passes only fixed actor `scheduled-cleanup` and limit `50` to `processDueProductAssetCleanupJobs`; no request body/query storage path is accepted. Project success/failure to stable `ok`, `status` or `reason`, and aggregate `claimed`, `cleaned`, `failed` counts only; exclude items, paths, provider errors, and service-role detail.</action>
  <verify><automated>corepack pnpm --filter @soji/web test -- tests/cron-auth.test.ts tests/product-asset-cleanup-cron-route.test.ts</automated></verify>
  <acceptance_criteria>
    - Every missing/malformed/incorrect secret has identical status, JSON shape, and no-store header.
    - Only an exact Bearer value with valid configured secret reaches the executor.
    - Executor call has fixed actor/limit and no caller-supplied path.
    - Response key assertions reject storage path, items, error text, signed URL, or service detail.
  </acceptance_criteria>
  <done>Scheduler authorization and output are deterministic, minimal, and secret-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove lease failure and retry semantics through the shared executor</name>
  <files>apps/web/src/lib/product-asset-cleanup.ts, apps/web/tests/product-asset-cleanup.test.ts, apps/web/tests/product-asset-cleanup-cron-route.test.ts</files>
  <read_first>apps/web/src/lib/product-asset-cleanup.ts, apps/web/tests/product-asset-cleanup.test.ts, apps/web/tests/product-asset-cleanup-cron-route.test.ts, supabase/migrations/20260714080000_lease_product_asset_cleanup.sql, supabase/migrations/20260714070000_service_product_asset_cleanup.sql</read_first>
  <action>Add RED tests for no due jobs, successful claims, expired lease reclaim, Storage remove failure, attempt-record failure, and claim failure. Preserve one shared executor for Admin and scheduler. Storage failure must call the durable attempt recorder with a retryable failed state and safe stable reason; a later run may reclaim it. Claim or attempt-record failure returns `ok: false` and the cron route returns 500. Success reports aggregate claimed/cleaned/failed counts. Operational logging may contain stable job/reference identifiers locally but neither route output nor evidence may contain storage paths or raw Supabase/Storage messages.</action>
  <verify><automated>corepack pnpm --filter @soji/web test -- tests/product-asset-cleanup.test.ts tests/product-asset-cleanup-cron-route.test.ts tests/admin-product-asset-cleanup-route.test.ts</automated></verify>
  <acceptance_criteria>
    - Both Admin and cron route tests spy on the same exported executor.
    - Expired processing leases are reclaimable and Storage failure remains a retryable durable job.
    - Claim and attempt-record failure produce `ok: false` and scheduler 500.
    - Aggregate counts match the controlled jobs and no protected detail crosses the route.
  </acceptance_criteria>
  <done>Cleanup outcome truth survives partial provider failure and remains safely retryable.</done>
</task>

</tasks>

<verification>
- [ ] Cron auth, scheduler route, shared executor, and Admin route tests pass.
- [ ] Auth denials and aggregate result schemas are stable and secret-safe.
- [ ] Failure/lease behavior remains durable and retryable.
</verification>

<success_criteria>
- OPS-02 scheduled cleanup behavior is complete locally; only the real production scheduler observation remains external.
</success_criteria>

<output>Create `04-03-SUMMARY.md`.</output>

