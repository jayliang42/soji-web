---
phase: 04-experience-and-operations-acceptance
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/observability.ts
  - apps/web/src/lib/env.ts
  - apps/web/src/lib/admin-launch-checklist.ts
  - apps/web/tests/observability.test.ts
  - apps/web/tests/env.test.ts
  - apps/web/tests/admin-launch-checklist.test.ts
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "D-11: the local operational record is created before optional secondary delivery and remains authoritative."
    - "D-12 D-13: the v1 webhook envelope is allowlisted, bounded, redacted, HTTPS-only, timeout-bounded, and redirect-refusing."
    - "D-14 D-15: delivery failure logs one stable local code without recursion or mutation of the business result; controlled receiver tests are not production evidence."
  artifacts:
    - path: "apps/web/src/lib/observability.ts"
      provides: "Versioned allowlisted alert envelope and bounded delivery"
    - path: "apps/web/tests/observability.test.ts"
      provides: "Exact payload, redaction, redirect, timeout, and failure isolation proof"
  key_links:
    - from: "reportOperationalError"
      to: "operations receiver"
      via: "createOperationsAlertEnvelope and fetch"
      pattern: "schemaVersion"
---

<objective>
Make payment and operational failure alerts safe, versioned, bounded, and testable while
preserving local-first failure truth.
</objective>

<execution_context>
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/workflows/execute-plan.md
@/Users/liangzhisong/PersonalProject/soji-web/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-experience-and-operations-acceptance/04-CONTEXT.md
@.planning/phases/04-experience-and-operations-acceptance/04-RESEARCH.md
@.planning/phases/04-experience-and-operations-acceptance/04-VALIDATION.md
</context>

<threat_model>
asvs_baseline: "OWASP ASVS L1 — V5 Validation, V8 Data Protection, V10 Malicious Code, V14 Configuration"
high_severity_gate: "STOP if an external alert can contain arbitrary error/context, credentials, customer data, provider payloads, signed URLs, or redirected delivery."
threats:
  - threat_id: T-04-03
    category: Information Disclosure / SSRF
    severity: HIGH
    component: "Operations alert envelope and receiver"
    disposition: mitigate
    mitigation_plan: "Allowlist projection, HTTPS URL validation, no credentials, redirect error, fixed timeout."
  - threat_id: T-04-04
    category: Availability / Recursive Failure
    severity: HIGH
    component: "Secondary alert delivery"
    disposition: mitigate
    mitigation_plan: "Local log first, secondary failure logs locally only, no recursive report call."
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the versioned redacted operations-alert contract</name>
  <files>apps/web/src/lib/observability.ts, apps/web/tests/observability.test.ts</files>
  <read_first>apps/web/src/lib/observability.ts, apps/web/tests/observability.test.ts, apps/web/src/app/api/webhooks/stripe/route.ts, apps/web/src/lib/product-asset-cleanup.ts, .planning/phases/04-experience-and-operations-acceptance/04-RESEARCH.md</read_first>
  <action>Add RED tests and implement an exported pure `createOperationsAlertEnvelope` returning exactly `schemaVersion: 1`, stable `eventCode`, `severity`, `subsystem`, `occurredAt`, `environment`, `retryable`, and optional bounded `reference`. Map subsystem/retryability from explicit inputs or stable event-prefix rules; truncate the reference to a documented bound and allow only a conservative character set. The envelope must have no `error`, arbitrary `context`, email, headers, cookies, tokens, secrets, URLs, raw messages, provider payloads, response bodies, or full provider/database IDs. Preserve `OperationalLog` for local logging and construct the external body only from the allowlisted envelope.</action>
  <verify><automated>corepack pnpm --filter @soji/web test -- tests/observability.test.ts</automated></verify>
  <acceptance_criteria>
    - Exact object-key assertions permit only the seven required fields plus optional `reference`.
    - Tests inject email/token/cookie/header/URL/provider-message sentinels and none serialize externally.
    - `schemaVersion` is numeric `1`; occurrence time is UTC ISO; event/subsystem/severity/retryability use bounded unions.
    - Local log still contains the stable event and is emitted before fetch.
  </acceptance_criteria>
  <done>External alert delivery receives a small allowlisted contract, not a serialized diagnostic object.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound secondary delivery and expose truthful readiness</name>
  <files>apps/web/src/lib/observability.ts, apps/web/src/lib/env.ts, apps/web/src/lib/admin-launch-checklist.ts, apps/web/tests/observability.test.ts, apps/web/tests/env.test.ts, apps/web/tests/admin-launch-checklist.test.ts</files>
  <read_first>apps/web/src/lib/observability.ts, apps/web/src/lib/env.ts, apps/web/src/lib/admin-launch-checklist.ts, apps/web/tests/observability.test.ts, apps/web/tests/env.test.ts, apps/web/tests/admin-launch-checklist.test.ts</read_first>
  <action>Add RED cases for missing/invalid receiver, URL credentials, non-HTTPS, redirect, timeout, thrown fetch, and non-2xx. Keep one receiver validator shared by runtime and launch checklist. Send with `Content-Type: application/json`, `redirect: "error"`, and `AbortSignal.timeout(2000)`. On any delivery failure, emit exactly one `operations.alert_delivery_failed` local warning containing only source event and optional HTTP status; never call `reportOperationalError` recursively. Return `{ alerted: false, log }` without changing the caller's business outcome. Make Admin readiness distinguish not configured from invalid without printing the destination.</action>
  <verify><automated>corepack pnpm --filter @soji/web test -- tests/observability.test.ts tests/env.test.ts tests/admin-launch-checklist.test.ts</automated></verify>
  <acceptance_criteria>
    - Fetch options assert HTTPS target from validator, `redirect: "error"`, JSON content type, POST, and 2000ms abort signal.
    - Missing or invalid config performs no fetch and Admin names the corrective action without destination output.
    - Redirect/timeout/throw/non-2xx logs one stable secondary code and does not recursively fetch.
    - Original local event and caller return semantics remain unchanged.
  </acceptance_criteria>
  <done>Secondary delivery is safe and readiness is explicit without claiming live delivery.</done>
</task>

</tasks>

<verification>
- [ ] Observability, environment, and Admin readiness tests pass.
- [ ] External alert body contains no arbitrary diagnostic properties or sensitive sentinel.
- [ ] Controlled receiver success/failure is automated proof only.
</verification>

<success_criteria>
- OPS-02 alert behavior is complete locally; only the canonical production receiver observation remains external.
</success_criteria>

<output>Create `04-02-SUMMARY.md`.</output>

