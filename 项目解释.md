# Soji 项目解释

## 项目目标

这个仓库是一个会员内容平台的技术骨架，目标是同时支持：

- Web 官网和会员站
- App 端内容消费
- 分级会员体系
- Stripe Web 支付
- App 内购接入能力
- 统一账号、统一权益、统一内容权限

当前优先级不是高并发，而是先把产品基础设施搭稳：

- 用户能注册登录
- 用户能按会员等级解锁不同内容
- Web 和 App 使用同一套会员规则
- 后台可以管理内容、模板、案例、office hour

---

## 当前整体架构

仓库采用 monorepo 结构，主要分为四层：

- `apps/web`
- `apps/app`
- `packages/*`
- `supabase`

### 1. `apps/web`

这是 Next.js Web 应用，负责：

- 官网展示
- 价格页
- 内容库
- 内容详情页
- 账户页
- Admin 后台壳子
- Stripe API 路由
- Supabase SSR 会话处理

### 2. `apps/app`

这是 Expo App 壳子，负责后续移动端能力：

- 登录
- 会员权益展示
- 内容浏览
- App 内购接入

目前 App 还是 mock 数据，但结构已经对齐 Web。

### 3. `packages/*`

这是共享代码层，目的是让 Web 和 App 使用同一套业务语言和规则：

- `packages/types`：共享类型
- `packages/domain`：共享领域规则
- `packages/ui`：共享品牌 token 和后续 UI 预留

### 4. `supabase`

这是数据库和后端数据结构基线，定义了：

- 用户资料
- 用户角色
- 会员套餐
- 权益映射
- 订阅记录
- 内容表
- 单次购买
- office hour

---

## 共享层说明

### `packages/types/src/index.ts`

这里定义共享类型，是整个系统的数据词典。

主要包括：

- `MembershipTier`
- `EntitlementKey`
- `ContentItem`
- `UserProfile`
- `OfficeHourSession`

作用：

- Web 和 App 都用同一套字段和类型
- 避免不同端各自发明命名
- 给 API、数据库映射、页面渲染提供统一约束

### `packages/domain/src/plans.ts`

这里是共享领域逻辑，不只是静态数据。

主要内容：

- `membershipPlans`：三个会员等级定义
- `marketingHighlights`：首页亮点文案
- `sampleLibrary`：当前 mock 内容
- `officeHourSessions`：当前 mock office hour
- `adminMetrics`：后台 mock 指标
- `getPlanByTier()`：按 tier 找套餐
- `getDefaultEntitlements()`：按 tier 获取默认权益
- `hasEntitlement()`：判断是否有权限访问内容

作用：

- 权限逻辑不散落在页面里
- Web 和 App 共用同一套会员规则
- 后面接数据库时，只需要替换数据源，不需要推翻规则层

### `packages/ui/src/index.ts`

当前只有品牌主题 token，作用是给后面抽共享样式和跨端设计系统留位置。

---

## Web 应用说明

### 页面层

#### `apps/web/src/app/layout.tsx`

整个站点的根布局：

- 顶部导航
- 全局页面框架
- 统一页面入口

#### `apps/web/src/app/page.tsx`

首页，负责：

- 展示产品定位
- 渲染营销信息
- 展示三个会员卡片

#### `apps/web/src/app/pricing/page.tsx`

价格页，负责：

- 渲染会员套餐
- 作为未来付费转化页面基础

#### `apps/web/src/app/library/page.tsx`

内容库页面，负责：

- 读取内容列表
- 读取当前用户权益
- 判断每条内容是否可访问

#### `apps/web/src/app/library/[slug]/page.tsx`

内容详情页，负责：

- 根据 slug 取内容
- 根据 entitlement 判断能否查看正文

#### `apps/web/src/app/account/page.tsx`

账户页，负责：

- 显示当前用户信息
- 显示当前会员等级
- 显示当前 entitlements
- 显示 session 来源

#### `apps/web/src/app/admin/page.tsx`

Admin 壳子，当前是占位实现：

- 展示指标
- 展示内容队列
- 展示 office hour 列表

后续会变成真正的内容管理后台。

#### `apps/web/src/app/login/page.tsx`

登录准备页，当前作用：

- 告诉你 Supabase 还需要哪些配置
- 展示当前 session 状态

后续这里会接：

- 邮箱注册
- 邮箱登录
- Google 登录

#### `apps/web/src/app/auth/callback/route.ts`

Supabase OAuth 回调路由，负责：

- 接收 `code`
- 调用 `exchangeCodeForSession`
- 把用户重定向回账户页或原始目标页

---

## Web 组件说明

### `apps/web/src/components/section-shell.tsx`

通用 section 容器组件，负责：

- 标题结构
- 文案布局
- 页面区块统一样式

### `apps/web/src/components/plan-card.tsx`

会员卡片组件，负责渲染单个套餐：

- 套餐名称
- 月费
- 权益列表
- featured 状态

### `apps/web/src/components/content-card.tsx`

内容卡片组件，负责：

- 渲染内容摘要
- 显示标签
- 显示锁定/可访问状态

### `apps/web/src/components/admin-metrics.tsx`

后台统计卡片组件。

### `apps/web/src/components/auth-status.tsx`

认证状态组件，负责：

- 显示当前 session 来自 Supabase 还是 demo
- 显示用户是否已登录
- 提供登录入口

---

## Web 服务层说明

### `apps/web/src/lib/env.ts`

环境变量层，负责：

- 读取 Supabase 配置
- 读取 Stripe 配置
- 读取站点 URL
- 判断当前是否完成 Supabase / Stripe 配置

### `apps/web/src/lib/content.ts`

当前内容读取层，暂时还是读取 mock 数据。

后续要改成：

- 从 Supabase 读取 `content_items`
- 从 `content_access_rules` 判断内容访问要求

### `apps/web/src/lib/stripe.ts`

Stripe client 初始化层，作用是给 API routes 使用。

### `apps/web/src/lib/session.ts`

这是 session 的统一导出层。

作用：

- 页面统一从这里取用户和权限
- 避免页面直接依赖具体 Supabase 实现

### `apps/web/src/lib/supabase/server.ts`

服务端 Supabase client：

- 给 server components 使用
- 给 route handlers 使用
- 处理 SSR cookie

### `apps/web/src/lib/supabase/browser.ts`

浏览器端 Supabase client：

- 给登录页、OAuth 按钮、前端交互使用

### `apps/web/src/lib/supabase/session.ts`

当前最关键的业务桥接层。

负责：

- 从 Supabase Auth 读取当前用户
- 从 `profiles` 读取用户资料
- 从 `user_roles` 读取角色
- 从 `user_entitlements` 读取额外权益
- 与 `tier` 默认权益合并
- 生成最终 session snapshot

如果没有配置 Supabase 环境变量，就回退 demo 用户。

这个设计的目的：

- 保证本地开发不中断
- 先让页面层稳定
- 再逐步切换到真实后端

### `apps/web/middleware.ts`

Next.js 中间件，负责在请求过程中维护 Supabase SSR session。

作用：

- 让服务端页面能稳定感知登录态
- 支撑 OAuth 登录和 session 刷新

---

## API 路由说明

### `apps/web/src/app/api/me/route.ts`

返回：

- 当前用户
- 当前 entitlements
- session 来源

作用：

- 给前端调试
- 给后续 app/web 统一资料接口提供基础

### `apps/web/src/app/api/checkout/subscription/route.ts`

创建 Stripe 订阅结账 session。

当前状态：

- 骨架已完成
- 还没写入数据库

### `apps/web/src/app/api/checkout/product/route.ts`

创建单次数字产品支付 session。

### `apps/web/src/app/api/webhooks/stripe/route.ts`

接 Stripe webhook。

当前状态：

- 已做签名校验骨架
- 后续需要把事件写入数据库
- 并同步到 `subscriptions` / `purchases` / `user_entitlements`

### `apps/web/src/app/api/webhooks/revenuecat/route.ts`

预留给 App 订阅 webhook。

---

## App 应用说明

### `apps/app/app/_layout.tsx`

Expo Router 根布局。

### `apps/app/app/index.tsx`

App 首页，展示产品亮点和跳转入口。

### `apps/app/app/membership.tsx`

会员页，展示：

- 当前会员等级
- 所有套餐
- 当前 entitlements

### `apps/app/app/library.tsx`

内容库页，展示：

- 内容列表
- 是否解锁

### `apps/app/src/lib/mock-session.ts`

App 当前 mock 数据来源。

后续要换成：

- Supabase 登录态
- 或统一 API 返回结果

### `apps/app/src/components/screen-shell.tsx`

App 通用页面容器。

---

## 数据库设计说明

数据库基线在：

- `supabase/schema.sql`

核心表说明如下。

### `profiles`

用户资料表，和 `auth.users` 关联。

用于存：

- 邮箱
- 昵称
- 头像
- 当前 tier
- 时区

### `user_roles`

用户角色表。

用于区分：

- 普通会员
- 编辑
- 管理员

### `membership_plans`

会员套餐表。

### `entitlements`

权益定义表。

例如：

- `content.basic`
- `library.templates`
- `office_hours.join`

### `plan_entitlements`

套餐和权益的映射表。

这是核心设计之一：

- 不是在代码里写死 “Tier 2 能看什么”
- 而是让套餐映射到权益

### `subscriptions`

订阅记录表，兼容：

- Stripe
- App Store
- Google Play

### `user_entitlements`

用户实际拥有的权益。

来源可以包括：

- 会员套餐
- 一次性购买
- 手动赠送

### `content_items`

内容表。

用于存：

- 标题
- 摘要
- 正文
- 内容类型
- 发布时间

### `content_access_rules`

内容访问规则表。

作用：

- 某条内容要求哪些 entitlement

### `products`

数字产品表。

### `purchases`

单次购买记录表。

### `office_hour_sessions`

office hour 活动信息。

---

## 当前已经完成了什么

目前仓库已经完成：

- Monorepo 基础结构
- Web 应用骨架
- App 应用骨架
- 共享类型层
- 共享领域层
- Supabase schema 基线
- Stripe API 路由骨架
- RevenueCat webhook 骨架
- Web 端 Supabase SSR session 基础设施
- Demo fallback 机制

目前还没完成的关键事项：

- 真实邮箱登录
- Google 登录按钮与流程
- `profiles` 自动创建
- 真实内容读取
- Stripe webhook 落库
- Admin CRUD
- App 端真实登录和订阅同步

---

## 当前 session 机制是怎么工作的

现在账户体系分两种情况：

### 情况 1：没有配置 Supabase 环境变量

系统会使用 demo 用户：

- `member@soji.club`
- `tier_2`
- 预设 entitlements

这样做的作用是：

- 仓库可以直接跑
- 页面开发不被后端阻塞

### 情况 2：配置了 Supabase

系统会：

1. 从 Supabase Auth 读取当前登录用户
2. 从 `profiles` 读取用户资料
3. 从 `user_roles` 读取角色
4. 从 `user_entitlements` 读取额外权益
5. 和当前 tier 的默认权益合并
6. 生成统一 session snapshot

页面层只消费这个 snapshot，不直接自己拼权限逻辑。

---

## 接下来你需要做什么

当前应该按这个顺序推进。

### 第一步：配置 Supabase

你需要：

1. 创建 Supabase 项目
2. 执行 `supabase/schema.sql`
3. 执行 `supabase/seed.sql`
3. 在 `apps/web/.env.local` 填写：
   - `NEXT_PUBLIC_SITE_URL`
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. 在 Supabase Auth 开启 Google Provider
5. 配置回调地址：
   - `http://localhost:3000/auth/callback`

如果你希望用自己的主账号发布内容：

1. 先用主账号注册一次
2. 修改 `supabase/publisher-setup.sql` 里的邮箱
3. 执行这份 SQL

这样你的账号会被提升为：

- `editor`
- `admin`

后续网站和 App 都应该读取同一份 Supabase 内容源，而不是各自发内容。

### 第二步：验证 session 接通

你需要打开：

- `/login`
- `/account`

确认 session 来源从 `Demo fallback` 变成 `Supabase`。

### 第三步：做真实登录功能

下一步要开发：

- 邮箱注册
- 邮箱登录
- Google 登录
- 登录后自动创建 `profiles`

### 第四步：接 Stripe

之后再做：

- 会员订阅 checkout
- 单次数字产品购买
- Stripe webhook 落库
- 订阅和购买同步到 `user_entitlements`

### 第五步：接真实内容

把内容层从 mock 改成数据库：

- 列表页查 Supabase
- 详情页查 Supabase
- 权限按 entitlement 判断

### 第六步：做最小 Admin

先做最小够用后台：

- 新建内容
- 编辑内容
- 设置 required entitlements
- 上传模板附件
- 创建 office hour

### 第七步：做 App 真实接入

最后再推进：

- App 登录
- App 内容同步
- RevenueCat 接入
- Web 和 App 权益统一

---

## 建议的开发节奏

推荐按下面的顺序，不要乱：

1. Supabase 项目配置
2. 登录功能
3. 账户页显示真实用户
4. Stripe 接入
5. 内容权限接数据库
6. Admin 后台
7. App 登录与同步
8. App 内购

原因很简单：

- 如果先做 App 和支付，复杂度会爆炸
- 先把 Web + 账号 + 权限跑通，后面扩到 App 会稳很多

---

## 一句话总结

现在这个仓库已经不是空壳了，它已经具备了：

- 一套可以同时服务 Web 和 App 的会员平台基础架构
- 一套以 entitlement 为中心的权限模型
- 一套可扩展到 Stripe 和 App 内购的支付骨架
- 一套可接 Supabase 的真实账户基础设施

接下来最关键的工作，不是再搭新架子，而是把：

- 登录
- 用户资料
- 真实内容
- 支付结果

一步一步接入到这套架构里。
